# Madde 132 · Tur 2 (uygulama) — Plan

**Tasarım:** [2026-08-29-queenagent-m132-replace-all-uygulama-design.md](../specs/2026-08-29-queenagent-m132-replace-all-uygulama-design.md)

## A. `tools.py` · `_edit`: bayrak okunur, `found > 1` ikiye ayrılır, ret cümlesi ikinci yolu gösterir.

## B. `tools.py` · `_edit`: birden çok yer değiştiyse sayı cevaba ve karta geçer.

## C. `tools.py` · `edit_file` tanımı: `replace_all` parametresi (zorunlu değil) ve açıklamada bir cümle.

## D. İki komut koşulur; beklenen yeşil: dört kırmızı döner, üç bekçi yeşil kalır, defter çifti bilinen kırmızı.

## E. Yeşil commit, ardından okuma kopyası.

## Bilerek yapılmayanlar: `found == 0` sırası, boş `old`, ön yüz, `dist`.
