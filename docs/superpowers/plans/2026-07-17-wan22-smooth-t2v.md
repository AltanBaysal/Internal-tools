# WAN 2.2 Smooth T2V — Deneme Ortamı Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `video_experiments/wan22-smooth-t2v/` klasörü + Colab notebook'u; notebook ortamı kurar (ComfyUI + 16 custom node + ~37 GiB model + cloudflared tüneli), kullanıcı workflow'u UI'da elle yükleyip TEXT2VIDEO grubunu çalıştırır.

**Architecture:** Workflow'a **hiç dokunulmaz** — yazarın 4 gruplu (`TEXT2VIDEO` / `IMAGE2VIDEO` / `FIRST2LASTFRAME` / `AUDIO2VIDEO`) UI grafiği olduğu gibi taşınır. Notebook hibrit: doğrulama+indirme `video_generator/imageToVideo.ipynb`'den (üç durumlu `check_safetensors` — NOTEBOOK-STANDARD.md §3 standardı), Civitai auth+probe ve tünel `video_experiments/ltx23-eros/ltx23-eros.ipynb`'den (§4 referansı).

**Tech Stack:** Google Colab (A100), ComfyUI, cloudflared, aria2c/curl, Civitai gated download (cookie), HuggingFace.

## Global Constraints

- **Branch: `feat/wan22-smooth-t2v`** (açıldı). 7 commit buraya gider; main'e merge Colab doğrulaması (Task 8) geçtikten sonra — doğrulanmamış 37 GiB'lık notebook main'de durmaz.
- **`docs/superpowers/` COMMIT EDİLMEZ** (kullanıcı kararı: dosyalar diskte dursun, repoya girmesin). Untracked kalır. Her commit `git add` ile **açık yol** verir; asla `git add .` / `git add -A` kullanma — spec/plan'ı içeri süpürür.
- **Workflow.json değiştirilmez.** Tek byte bile. Taşınır ve yeniden adlandırılır, o kadar.
- **Dil** (kök `CLAUDE.md`): markdown hücreleri + runtime çıktısı (`print`/`log`/`assert`/`RuntimeError`) **Türkçe**; kod yorumları (`#`) + docstring'ler **İngilizce**.
- **Civitai auth yalnızca yeni desen:** host `civitai.red`, cookie `__Secure-civ-token`, `?token=` API key YOK. `imageToVideo`'daki eski desen (`__Secure-civitai-token` + `civitai.com` + `CIVITAI_API_KEY`) **kopyalanmaz**.
- **Fail-loud:** bozuk/eksik indirme, başlamayan ComfyUI → `RuntimeError` + komutun ham çıktısı. Sebep uydurma.
- **Drive yok** — `video_experiments/` Drive kullanmaz; modeller her oturumda kaynaktan iner.
- **Doğrulama standardı:** üç durumlu `check_safetensors()` + `fetch(url, target_dir, filename, label, *, parallel, headers)`. `ltx23-eros`'un `is_valid_safetensors()` varyantı **kullanılmaz** (13.5 GB'lık kesik indirmeyi yakalamaz).
- **Test döngüsü gerçeği:** burada birim test yok. Lokalde doğrulanabilen tek şey dosya bütünlüğü/JSON geçerliliği. Asıl doğrulama Colab'da elle çalıştırmadır (Task 8) — plan bunu yalanlamaz.

---

### Task 1: Klasör iskeleti + workflow.json taşıma + backlog maddesi

**Files:**
- Create: `collab-toolbox/video_experiments/wan22-smooth-t2v/workflow.json` (taşınan)
- Delete: `WAN 2.2 Smooth Workflow v5.0 (1).json` (repo kökü)
- Modify: `collab-toolbox/video_experiments/deneme-listesi.md`

**Interfaces:**
- Produces: `workflow.json` — sonraki tüm task'ların referans aldığı dosya. İçindeki node id'leri: `37`/`56` (T2V UNETLoader), `38` (CLIPLoader), `39` (VAELoader), `50` (WanImageToVideo, boş latent), `109`/`110` (Power Lora Loader — BOŞ), `230:229` (PromptGenerator).

- [ ] **Step 1: Taşımadan önce hash al**

```bash
cd "d:/code/github/internal-tools"
sha256sum "WAN 2.2 Smooth Workflow v5.0 (1).json"
```
Çıktıyı not et — Step 3'te karşılaştırılacak.

- [ ] **Step 2: Taşı**

Repo kuralı gereği shell komutu: dosya taşımak için özel araç yok.

**`git mv` KULLANMA** — kaynak dosya untracked (`git status` → `?? "WAN 2.2 Smooth Workflow v5.0 (1).json"`), `git mv` "not under version control" hatası verir. Düz `mv` + `git add` doğrusu.

```bash
cd "d:/code/github/internal-tools"
mkdir -p collab-toolbox/video_experiments/wan22-smooth-t2v
mv "WAN 2.2 Smooth Workflow v5.0 (1).json" collab-toolbox/video_experiments/wan22-smooth-t2v/workflow.json
```

- [ ] **Step 3: İçerik değişmedi mi doğrula**

```bash
cd "d:/code/github/internal-tools"
sha256sum collab-toolbox/video_experiments/wan22-smooth-t2v/workflow.json
python -c "import json; d=json.load(open('collab-toolbox/video_experiments/wan22-smooth-t2v/workflow.json')); print('nodes:', len(d['nodes']), 'last_node_id:', d['last_node_id'])"
```
Beklenen: hash Step 1 ile **aynı**; `last_node_id: 388`. Hash farklıysa DUR — dosya bozulmuş.

- [ ] **Step 4: deneme-listesi.md'ye 6. madde ekle**

`## Denenecek workflow'lar / modeller` bölümünün SONUNA, mevcut madde anatomisine birebir uyarak:

```markdown
- [ ] **WAN 2.2 T2V — "Smooth Workflow v5.0" TEXT2VIDEO grubu (Digital Pastel)**
  - Kaynak: https://civitai.red/models/1995784/smooth-mix-wan-22-14b-i2vt2v
  - Model ailesi: WAN 2.2 **T2V**-A14B (SmoothMix T2V v3 High+Low)
  - Neden: listedeki ilk text-to-video denemesi — mevcut her şey I2V. `imageToVideo.json` zaten bu grafiğin FIRST2LASTFRAME grubu; T2V grubu hep vardı, hiç açılmadı. Sampler/çözünürlük/süre/RIFE ayarları I2V ile birebir aynı → kıyas temiz, değişen tek eksen I2V→T2V.
  - Durum: bekliyor
  - Not: —
```

- [ ] **Step 5: Commit**

```bash
cd "d:/code/github/internal-tools"
git add collab-toolbox/video_experiments/wan22-smooth-t2v/workflow.json collab-toolbox/video_experiments/deneme-listesi.md
git commit -m "feat(video_experiments): add wan22-smooth-t2v folder with the author's v5.0 graph

The uploaded WAN 2.2 Smooth Workflow v5.0 graph, moved in untouched. It carries
four groups (TEXT2VIDEO / IMAGE2VIDEO / FIRST2LASTFRAME / AUDIO2VIDEO); we drive
TEXT2VIDEO by hand in the UI.

Note video_generator/imageToVideo.json is an API export of this same graph's
FIRST2LASTFRAME group -- node ids line up 1:1, so T2V was always there.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: `indirilecekler.md` — indirme manifesti

**Files:**
- Create: `collab-toolbox/video_experiments/wan22-smooth-t2v/indirilecekler.md`

**Interfaces:**
- Consumes: Task 1'in `workflow.json`'u.
- Produces: Task 5 (custom node listesi) ve Task 6 (model listesi) bu dosyayı birebir kaynak alır. Buradaki her ad/ID notebook'a aynen geçer.

**Neden önce bu:** manifest notebook'un spesifikasyonudur. Önce yazılır ki notebook'taki liste "tahmin" değil, doğrulanmış bir belgeden kopya olsun.

- [ ] **Step 1: Dosyayı yaz**

Mevcut `wan22-dasiwa/indirilecekler.md` iskeletine uy (etiket satırı → blockquote → numaralı bölümler):

```markdown
# WAN 2.2 Smooth T2V — Colab'da indirilecekler (workflow.json'dan çıkarıldı)

Etiketler: **✅** API'den doğrulandı · **📌** workflow.json'a gömülü (yazar verdi) · **📚** kanonik repo (yaygın, ping'lenmedi) · **⚠️** bilinen risk.

> Grafiğin TEXT2VIDEO grubu indirilir. Diğer 3 grup (IMAGE2VIDEO / FIRST2LASTFRAME / AUDIO2VIDEO) grafikte durur ama modelleri **inmez** — açılırsa model bulunamaz hatası verir.

## 1) Custom node'lar (16)

`video_generator/imageToVideo.ipynb`'nin listesi birebir — aynı grafiğin FIRST2LASTFRAME grubunu çalıştıran kanıtlanmış liste. Tam grafik yüklendiği için hepsi şart: bypass'lı grubun node class'ı yoksa UI "missing node" verir.

| # | Paket | git URL | Sağladığı |
|---|---|---|---|
| 1 | ComfyUI-Manager | https://github.com/ltdrdata/ComfyUI-Manager.git | eksik node tespiti |
| 2 | rgthree-comfy | https://github.com/rgthree/rgthree-comfy.git | Power Lora Loader, Seed, Fast Groups Bypasser |
| 3 | comfy_mtb | https://github.com/melMass/comfy_mtb.git | Note Plus, Pick From Batch, RIFEInterpolation |
| 4 | ComfyUI-VideoHelperSuite | https://github.com/Kosinkadink/ComfyUI-VideoHelperSuite.git | VHS_VideoCombine |
| 5 | ComfyUI-MMAudio | https://github.com/kijai/ComfyUI-MMAudio.git | AUDIO2VIDEO grubu (model inmez) |
| 6 | ComfyUI-WanVideoWrapper | https://github.com/kijai/ComfyUI-WanVideoWrapper.git | Wan video node'ları |
| 7 | ComfyUI-GGUF | https://github.com/city96/ComfyUI-GGUF.git | UnetLoaderGGUF |
| 8 | ComfyUI-KJNodes | https://github.com/kijai/ComfyUI-KJNodes.git | ImageResizeKJv2, ColorMatch |
| 9 | ComfyMath | https://github.com/evanspearman/ComfyMath.git | ComfyMathExpression (süre → kare) |
| 10 | ComfyUI-Frame-Interpolation | https://github.com/Fannovel16/ComfyUI-Frame-Interpolation.git | RIFE |
| 11 | ComfyUI-VFI | https://github.com/GACLove/ComfyUI-VFI.git | frame interpolation |
| 12 | ComfyUI_Comfyroll_CustomNodes | https://github.com/Suzie1/ComfyUI_Comfyroll_CustomNodes.git | CR Float To Integer |
| 13 | ComfyUI-Easy-Use | https://github.com/yolain/ComfyUI-Easy-Use.git | easy cleanGpuUsed |
| 14 | ComfyUI-mxToolkit | https://github.com/Smirnov75/ComfyUI-mxToolkit.git | mxSlider2D (çözünürlük) |
| 15 | ComfyUI-NAG | https://github.com/scottmudge/ComfyUI-NAG.git | KSamplerWithNAG |
| 16 | comfyui-adaptiveprompts | https://github.com/Alectriciti/comfyui-adaptiveprompts.git | PromptGenerator |

## 2) Modeller — gated Civitai (model 1995784, version ID'ler API ile doğrulandı ✅)

| Hedef | Kaydedilecek ad | Version ID | Civitai'nin verdiği ad | Boyut | Durum |
|---|---|---|---|---|---|
| diffusion_models | SmoothMix_T2V_High_v3.safetensors | **2768924** | smoothMixWan2214BI2V_t2vHighV30.safetensors | ~13.5 GiB | ✅ "T2V High v3.0", base `Wan Video 2.2 T2V-A14B` |
| diffusion_models | SmoothMix_T2V_Low_v3.safetensors | **2768944** | smoothMixWan2214BI2V_t2vLowV30.safetensors | ~13.5 GiB | ✅ "T2V Low v3.0", aynı base |
| loras | WAN_General_NSFW_HIGH.safetensors | **2073605** | NSFW-22-H-e8.safetensors | ~0.57 GiB | ⚠️ base `Wan Video 2.2 **I2V**-A14B` |
| loras | WAN_General_NSFW_LOW.safetensors | **2083303** | (v0.08a LOW) | ~0.57 GiB | ⚠️ base `Wan Video 2.2 **I2V**-A14B` |

> **Ad değişimi şart:** Civitai dosyayı `smoothMixWan2214BI2V_t2v*V30.safetensors` olarak veriyor; workflow'un UNETLoader 37/56'sı `SmoothMix_T2V_*_v3.safetensors` istiyor. `fetch()` hedef adla kaydeder — yapılmazsa dropdown'da model görünmez.

> **NSFW LoRA riski:** model 1307155'in dört sürümü de I2V-A14B; T2V sürümü yok. Kullanıcı isteğiyle iniyor. Çıktı bozuksa **önce bunu bypass et** — yoksa "T2V mi kötü, LoRA mı uyumsuz" ayrışmaz.

## 3) Modeller — HuggingFace

| Hedef | Kaydedilecek ad | Kaynak | Boyut | Durum |
|---|---|---|---|---|
| diffusion_models üstü: loras | wan2.2_t2v_lightx2v_4steps_lora_v1.1_high_noise.safetensors | Comfy-Org/Wan_2.2_ComfyUI_Repackaged `split_files/loras/` | 1.14 GiB | ✅ HF API (1,226,977,424 B) |
| loras | wan2.2_t2v_lightx2v_4steps_lora_v1.1_low_noise.safetensors | Comfy-Org/Wan_2.2_ComfyUI_Repackaged `split_files/loras/` | 1.14 GiB | ✅ HF API (1,226,977,424 B) |
| text_encoders | umt5_xxl_fp8_e4m3fn_scaled.safetensors | Comfy-Org/Wan_2.1_ComfyUI_repackaged `split_files/text_encoders/` | ~6.3 GiB | 📚 imageToVideo'da çalışan indirme |
| vae | **Wan2_1_VAE_fp32.safetensors** ← `wan_2.1_vae.safetensors` | Comfy-Org/Wan_2.1_ComfyUI_repackaged `split_files/vae/` | ~250 MB | 📚 imageToVideo deseni (indir + rename) |

## 4) Upscale modeli — YOK

Yazarın "Upscale by 2"si `ImageScaleBy` (lanczos) — algoritmik, dosya inmez. Grafikte `UpscaleModelLoader` yok (tam tarama: aşağıda).

## 5) Otomatik (manuel indirme yok)

| Dosya | Not |
|---|---|
| flownet.pkl | `RIFEInterpolation` (comfy_mtb) ilk kullanımda indirir. ⚠️ **Doğrulanmadı** — Colab run'da teyit edilecek; inmezse `indirilecekler.md` güncellenir ve notebook'a eklenir. |

## 6) İnmeyen (diğer gruplar için — bilerek atlandı)

`SmoothMix_I2V_v2_High/Low` (2513182/2513186), `clip_vision_h.safetensors`, MMAudio 4 dosya. IMAGE2VIDEO / FIRST2LASTFRAME / AUDIO2VIDEO gruplarını açarsan bunlar eksik olur.

## 7) Doğrulama (tam tarama)

Grafikteki loader node'ları: `UNETLoader` ×6, `UnetLoaderGGUF` ×6 (hepsi `unet_name: null` — boş, kullanılmıyor), `CLIPLoader` ×3, `VAELoader` ×3, `CLIPVisionLoader` ×1 (yalnız F2LF), `Power Lora Loader (rgthree)` ×6 (hepsi BOŞ), `MMAudioModelLoader`/`MMAudioFeatureUtilsLoader` ×4 (yalnız audio), `LoadImage` ×3 (I2V/F2LF), `RIFEInterpolation` ×4.

`UpscaleModelLoader` → **yok**. `controlnet` / `ipadapter` / `onnx` / `embeddings` → **yok**.

**TEXT2VIDEO grubunun (x: 524→3750, y: -61→1980) kullandığı loader'lar:** UNETLoader 37/56, CLIPLoader 38, VAELoader 39, Power Lora Loader 109/110 (boş), RIFEInterpolation 160 → hepsi yukarıdaki listeyle eşleşti.
```

- [ ] **Step 2: Tabloyu grafiğe karşı doğrula**

```bash
cd "d:/code/github/internal-tools/collab-toolbox/video_experiments/wan22-smooth-t2v"
grep -o 'SmoothMix_T2V_[A-Za-z]*_v3\.safetensors' workflow.json | sort -u
grep -o 'Wan2_1_VAE_fp32\.safetensors' workflow.json | sort -u
grep -o 'umt5_xxl_fp8_e4m3fn_scaled\.safetensors' workflow.json | sort -u
grep -c 'UpscaleModelLoader' workflow.json
```
Beklenen: ilk üçü hit verir (manifestteki adlar grafikle **birebir** aynı), `UpscaleModelLoader` sayımı **0**. Adlar tutmuyorsa manifest yanlış — DUR ve düzelt.

- [ ] **Step 3: Commit**

```bash
cd "d:/code/github/internal-tools"
git add collab-toolbox/video_experiments/wan22-smooth-t2v/indirilecekler.md
git commit -m "docs(wan22-smooth-t2v): download manifest extracted from the graph

Every Civitai version id and HF path was resolved against the APIs, not copied
from comments. Two things the manifest pins down:
- Civitai serves the checkpoints under smoothMixWan2214BI2V_t2v*V30.safetensors
  while the graph's UNETLoaders ask for SmoothMix_T2V_*_v3.safetensors
- there is no upscale model: 'Upscale by 2' is ImageScaleBy lanczos

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: `instructions.md` — kullanım

**Files:**
- Create: `collab-toolbox/video_experiments/wan22-smooth-t2v/instructions.md`

**Interfaces:**
- Consumes: Task 1 `workflow.json`, Task 2 manifest.

- [ ] **Step 1: Dosyayı yaz**

Mevcut `wan22-painter/instructions.md` iskeletine uy (başlık → metadata bullet'ları → blockquote → bölümler). **LoRA uyarısı en üstte durmalı** — atlanması en kolay ve en pahalı adım.

```markdown
# WAN 2.2 T2V — "Smooth Workflow v5.0" TEXT2VIDEO (Digital Pastel) — Talimatlar

- **Kaynak model:** https://civitai.red/models/1995784/smooth-mix-wan-22-14b-i2vt2v
- **Sürüm:** v5.0 — grafik yazarın orijinali, **değiştirilmedi**
- **Model ailesi:** WAN 2.2 T2V-A14B
- **Grafik:** 4 grup içerir; biz **TEXT2VIDEO**'yu kullanırız

> ⚠️ **En kritik adım — atlanırsa çıktı çöp olur.** Yazar Power Lora Loader'ları (**109** / **110**) BOŞ bırakmış, ama sampler **6 step / cfg 1**'e ayarlı. Bu ancak distill LoRA ile çalışır. Notebook `wan2.2_t2v_lightx2v_4steps_lora_v1.1_high/low_noise` dosyalarını indirir — **UI'da loader'lara elle eklemen gerekir.** Eklemezsen model kötü değil, ayar eksiktir.

## Ne yapar

Prompt'tan doğrudan video: 480×720, 5 sn, RIFE ile 16→32 fps, `ImageScaleBy` ile 2× upscale, h264-mp4. Görsel girdi yok — `WanImageToVideo` (node 50) `start_image` bağlanmadan kullanılıyor (WAN'ın boş-latent numarası).

## Kullanım

1. Runtime → **A100 GPU** (Colab Pro).
2. `wan22-smooth-t2v.ipynb` → CONFIG'de Civitai cookie'yi kontrol et → **Run all**. (~37 GiB iner.)
3. Son hücrenin bastığı `trycloudflare` linkine gir.
4. UI'da bu klasördeki **`workflow.json`**'u yükle (Workflow → Open, ya da dosyayı tarayıcıya sürükle).
5. **"Choose Your Workflow"** (Fast Groups Bypasser) → **TEXT2VIDEO**'yu aç. Grafik tamamen bypass'lı gelir; hiçbir grup açık değildir.
6. **Power Lora Loader 109/110'a lightx2v LoRA'larını ekle** (yukarıdaki uyarı). Sırasıyla HIGH → 109, LOW → 110.
7. **PromptGenerator** (node 230:229) boş gelir — prompt'unu yaz.
8. Model dropdown'larında `SmoothMix_T2V_High_v3` / `_Low_v3` seçili mi bak.
9. **Run**.

## Notlar / bilinen sorunlar

- **Sadece TEXT2VIDEO çalışır.** Diğer 3 grubun modelleri inmez (bkz. `indirilecekler.md` §6); açarsan model bulunamaz hatası alırsın.
- **NSFW LoRA I2V için eğitilmiş** (`I2V-A14B`; T2V sürümü yok). İniyor ama garantisi yok — çıktı bozuksa **önce onu bypass et**.
- Eksik custom node çıkarsa: UI'da **Manager → Install Missing Custom Nodes** → Restart. (Notebook `--enable-manager` ile başlar.)
- Başlatma hücresi bilerek açık kalır (log akar). **Kapatma** — kapanırsa Colab runtime'ı idle sayıp tüneli öldürür.

## Başka creator'ın modelini kullanmak

Notebook'taki `CIVITAI_MODELS` listesine tek satır ekle:

```python
(version_id, DIFF, "İstediğin_Ad.safetensors", "Etiket"),
```

Dosya UI'daki UNETLoader dropdown'ında görünür; **workflow'a dokunmana gerek yok**.

**Kısıt:** WAN 2.2 A14B bir MoE — grafikte iki `UNETLoader` olmasının sebebi bu. Alternatif model de (a) **`Wan Video 2.2 T2V-A14B`** base olmalı (I2V değil — NSFW LoRA'da bu tuzağa düştük), (b) **high/low noise çifti** halinde gelmeli. Tek dosyalık model, WAN 2.1 veya LTX bu grafiğe düşmez.
```

- [ ] **Step 2: Commit**

```bash
cd "d:/code/github/internal-tools"
git add collab-toolbox/video_experiments/wan22-smooth-t2v/instructions.md
git commit -m "docs(wan22-smooth-t2v): usage instructions

Leads with the LoRA warning: the author ships Power Lora Loaders 109/110 empty
while the sampler is set to 6 steps at cfg 1, which only works with a distill
LoRA. Skipping that step makes T2V look broken when it is only unconfigured.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: Notebook — başlık + CONFIG + helpers

**Files:**
- Create: `collab-toolbox/video_experiments/wan22-smooth-t2v/wan22-smooth-t2v.ipynb`
- Read (kaynak): `collab-toolbox/video_generator/imageToVideo.ipynb` cell 5, `collab-toolbox/video_experiments/ltx23-eros/ltx23-eros.ipynb` cell c2

**Interfaces:**
- Produces: `COOKIE_VALUE`, `COMFY_PORT` (int, 8188); helpers `log(msg, level="INFO")`, `human(b)`, `head_text(path, limit=4000)`, `run(cmd, label, cwd=None, timeout=3600) -> stdout`, `check_safetensors(path) -> ("ok"|"partial"|"invalid", msg)`. Task 5/6/7 bunları kullanır.

- [ ] **Step 1: Başlık hücresi (markdown) — Türkçe**

```markdown
# WAN 2.2 Smooth T2V — ComfyUI interaktif deneme (Colab)

**Input:** Prompt (ComfyUI UI'da) · **Output:** 480×720, 5 sn, 32 fps mp4

Sıra:
1. **CONFIG** — Civitai cookie
2. **Helpers** — log + fail-loud run + model doğrulama
3. **ComfyUI + Manager + custom node'lar** (16)
4. **Modeller** — önce gated probe, sonra indir (~37 GiB)
5. **Başlat + cloudflared tünel** → UI linki

> Drive kullanılmaz; modeller her oturumda kaynaktan iner (Colab ephemeral disk).
> **Runtime → Run all** → en alttaki linke gir → bu klasördeki `workflow.json`'u yükle →
> **"Choose Your Workflow" → TEXT2VIDEO** → **Power Lora Loader 109/110'a lightx2v LoRA'larını ekle** → prompt yaz → Run.
> LoRA'ları eklemezsen (6 step / cfg 1) çıktı çöp olur — bkz. `instructions.md`.
```

- [ ] **Step 2: CONFIG hücresi (kod)**

`COOKIE_VALUE`'yu `ltx23-eros/ltx23-eros.ipynb` cell c2'deki değerden **birebir kopyala** (o cookie `exp` ~2026-08-26'ya kadar geçerli). Cookie CONFIG'de yaşar ve commit edilir — bu projede kabul edilmiş pratik (`NOTEBOOK-STANDARD.md` §4).

```python
# === CONFIG ===
# Civitai login-gated download: civitai.red -> log in -> F12 -> Application -> Cookies ->
# paste the __Secure-civ-token value (double-click -> Ctrl+A -> Ctrl+C; a single click on the
# table cell truncates it and `assert len>200` still passes while the token is invalid).
# NOTE: auth moved to auth.civitai.com -> the cookie NAME is __Secure-civ-token (NOT the old
#   __Secure-civitai-token) and the value is a short ES256 JWT (~420 chars), not the old long JWE.
# Cookie only; never a ?token= API key -> gated assets answer 401. exp ~30 days -> log in again.
COOKIE_VALUE = "<ltx23-eros.ipynb c2'deki COOKIE_VALUE ile aynı>"

COMFY_PORT = 8188

import os
os.environ["COOKIE_VALUE"] = COOKIE_VALUE
assert len(COOKIE_VALUE) > 200, "❌ COOKIE_VALUE boş/çok kısa — civitai.red'den __Secure-civ-token (ES256 JWT) yapıştır"
print(f"✓ Cookie: {len(COOKIE_VALUE)} char")
print("=== GPU ===")
!nvidia-smi --query-gpu=name,memory.total,driver_version --format=csv,noheader
```

- [ ] **Step 3: Helpers markdown hücresi**

```markdown
## 2) Ortak Yardımcılar

`log` + fail-loud `run` + model doğrulama. Doğrulama ağa soru sormaz: safetensors header'ındaki `data_offsets` beklenen boyutu verir (HEAD/`Content-Length` kullanılmaz — HF'in Xet CDN'i imzalı URL'de HEAD'e 403 döner).
```

- [ ] **Step 4: Helpers kod hücresi**

`imageToVideo.ipynb` **cell 5'i birebir kopyala** — `log`, `human`, `head_text`, `run`, `check_safetensors` (docstring'leri dahil, İngilizce). Tek değişiklik son satır:

```python
print("✓ Ortak yardımcılar hazır (log, run, human, head_text, check_safetensors)")
```

**`ltx23-eros`'un `is_valid_safetensors`'ını KULLANMA** — o yalnızca "dosya >1MB + header magic" bakar; 13.5 GiB'lık kesik bir checkpoint'i "valid" sayar. Üç durumlu `check_safetensors` beklenen boyutu header'dan hesapladığı için kesik indirmeyi yakalar (`NOTEBOOK-STANDARD.md` §3).

- [ ] **Step 5: Notebook JSON geçerli mi**

```bash
cd "d:/code/github/internal-tools/collab-toolbox/video_experiments/wan22-smooth-t2v"
python -c "import json; nb=json.load(open('wan22-smooth-t2v.ipynb')); print('cells:', len(nb['cells']))"
```
Beklenen: `cells: 4`, hata yok.

- [ ] **Step 6: Commit**

```bash
cd "d:/code/github/internal-tools"
git add collab-toolbox/video_experiments/wan22-smooth-t2v/wan22-smooth-t2v.ipynb
git commit -m "feat(wan22-smooth-t2v): notebook config + shared helpers

Helpers come from imageToVideo (three-state check_safetensors), not ltx23-eros:
its is_valid_safetensors only checks header magic and size>1MB, which would call
a truncated 13.5GiB checkpoint valid. Civitai auth follows the new pattern.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Notebook — ComfyUI + custom node'lar

**Files:**
- Modify: `collab-toolbox/video_experiments/wan22-smooth-t2v/wan22-smooth-t2v.ipynb` (hücre ekle)

**Interfaces:**
- Consumes: `log()`, `run()` (Task 4).
- Produces: `/content/ComfyUI` + 16 custom node kurulu.

- [ ] **Step 1: Markdown hücresi**

```markdown
## 3) ComfyUI + Manager + Custom Node'lar (16)

Grafiğin tamamı (4 grup) yükleneceği için **hepsi** kurulur — bypass'lı grubun node class'ı yoksa UI "missing node" verir. Biri başarısız olursa hücre durur (fail-loud).
```

- [ ] **Step 2: Kod hücresi**

`imageToVideo.ipynb` cell 7'nin **CUSTOM_NODES listesi ve döngüsü birebir** (16 satır, inline yorumlarıyla). Tek fark: `%cd` yerine mutlak yol kullanma zorunluluğu yok, imageToVideo kalıbı korunur.

```python
%cd /content

# === System deps + ComfyUI ===
!apt-get install -y ffmpeg aria2 > /dev/null 2>&1
![ -d ComfyUI ] || git clone https://github.com/comfyanonymous/ComfyUI.git
%cd /content/ComfyUI
!git pull -q
!pip install -q -r requirements.txt
!pip install -q opencv-python imageio imageio-ffmpeg sageattention

# === Custom nodes (fail-loud: clone or pip failure -> RuntimeError) ===
import os
%cd /content/ComfyUI/custom_nodes

# (folder, repo) — trailing comment = what the node provides
CUSTOM_NODES = [
    ("ComfyUI-Manager",                 "https://github.com/ltdrdata/ComfyUI-Manager.git"),            # detect missing nodes
    ("rgthree-comfy",                   "https://github.com/rgthree/rgthree-comfy.git"),               # Power Lora Loader, Seed, Fast Groups Bypasser
    ("comfy_mtb",                       "https://github.com/melMass/comfy_mtb.git"),                   # Note Plus, Pick From Batch, RIFEInterpolation
    ("ComfyUI-VideoHelperSuite",        "https://github.com/Kosinkadink/ComfyUI-VideoHelperSuite.git"),# VHS_VideoCombine
    ("ComfyUI-MMAudio",                 "https://github.com/kijai/ComfyUI-MMAudio.git"),               # AUDIO2VIDEO group (models not downloaded)
    ("ComfyUI-WanVideoWrapper",         "https://github.com/kijai/ComfyUI-WanVideoWrapper.git"),       # Wan video nodes
    ("ComfyUI-GGUF",                    "https://github.com/city96/ComfyUI-GGUF.git"),                 # UnetLoaderGGUF (unused but present in the graph)
    ("ComfyUI-KJNodes",                 "https://github.com/kijai/ComfyUI-KJNodes.git"),               # ImageResizeKJv2, ColorMatch
    ("ComfyMath",                       "https://github.com/evanspearman/ComfyMath.git"),              # ComfyMathExpression (seconds -> frames)
    ("ComfyUI-Frame-Interpolation",     "https://github.com/Fannovel16/ComfyUI-Frame-Interpolation.git"),# RIFE
    ("ComfyUI-VFI",                     "https://github.com/GACLove/ComfyUI-VFI.git"),                 # frame interpolation
    ("ComfyUI_Comfyroll_CustomNodes",   "https://github.com/Suzie1/ComfyUI_Comfyroll_CustomNodes.git"),# CR Float To Integer
    ("ComfyUI-Easy-Use",                "https://github.com/yolain/ComfyUI-Easy-Use.git"),             # easy cleanGpuUsed
    ("ComfyUI-mxToolkit",               "https://github.com/Smirnov75/ComfyUI-mxToolkit.git"),         # mxSlider2D (resolution)
    ("ComfyUI-NAG",                     "https://github.com/scottmudge/ComfyUI-NAG.git"),              # KSamplerWithNAG
    ("comfyui-adaptiveprompts",         "https://github.com/Alectriciti/comfyui-adaptiveprompts.git"), # PromptGenerator
]

for name, url in CUSTOM_NODES:
    if os.path.exists(name) and os.listdir(name):
        log(f"{name}: zaten var")
        continue
    log(f"{name}: cloning...")
    run(["git", "clone", "--depth", "1", url, name], f"clone {name}", timeout=120)
    if not os.listdir(name):                 # clone reported success but folder is empty
        raise RuntimeError(f"{name}: klon sonrası klasör boş")
    req = f"/content/ComfyUI/custom_nodes/{name}/requirements.txt"
    if os.path.exists(req):
        run(f"pip install -q -r {req}", f"pip install {name}", timeout=300)  # install node deps

log(f"{len(CUSTOM_NODES)} custom node hazır", "OK")
```

- [ ] **Step 3: Liste manifestle eşleşiyor mu**

```bash
cd "d:/code/github/internal-tools/collab-toolbox/video_experiments/wan22-smooth-t2v"
grep -o 'https://github\.com/[A-Za-z0-9_.-]*/[A-Za-z0-9_.-]*\.git' wan22-smooth-t2v.ipynb | sort -u | wc -l
```
Beklenen: **17** (16 custom node + ComfyUI'nin kendisi). `indirilecekler.md` §1'deki 16 URL ile birebir aynı olmalı.

- [ ] **Step 4: Commit**

```bash
cd "d:/code/github/internal-tools"
git add collab-toolbox/video_experiments/wan22-smooth-t2v/wan22-smooth-t2v.ipynb
git commit -m "feat(wan22-smooth-t2v): ComfyUI + the 16 custom nodes the graph needs

The list is imageToVideo's verbatim -- it already runs this same graph's
FIRST2LASTFRAME group, so it is proven rather than guessed. All 16 are required
even though we only drive TEXT2VIDEO: a bypassed group whose node class is
missing still shows up as a missing-node error in the UI.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: Notebook — modeller (Civitai probe + indirme)

**Files:**
- Modify: `collab-toolbox/video_experiments/wan22-smooth-t2v/wan22-smooth-t2v.ipynb` (hücre ekle)

**Interfaces:**
- Consumes: `log()`, `run()`, `human()`, `head_text()`, `check_safetensors()` (Task 4); `COOKIE_VALUE` (Task 4).
- Produces: `fetch(url, target_dir, filename, label, *, parallel, headers=None)`, `civitai_url(version_id)`, `cookie_header()`, `civitai_probe(version_id, label)`; `/content/ComfyUI/models/**` dolu.

- [ ] **Step 1: Markdown hücresi**

```markdown
## 4) Modeller — önce gated probe, sonra indir (~37 GiB)

Gated erişim **ağır indirmeden önce** doğrulanır (ilk 1 KB). Herhangi bir dosya bozuk/eksik inerse hücre `RuntimeError` ile durur; bozuk dosya silinmez, inceleme için diskte kalır.

Civitai checkpoint'leri `smoothMixWan2214BI2V_t2v*V30.safetensors` adıyla gelir; workflow `SmoothMix_T2V_*_v3.safetensors` beklediği için o adla iner.
```

- [ ] **Step 2: Kod hücresi**

`fetch()` **`imageToVideo.ipynb` cell 9'dan birebir kopyalanır** (docstring dahil). `civitai_url`/`cookie_header` **yeni** desenle yeniden yazılır, `civitai_probe` `ltx23-eros` c3'ten uyarlanır.

```python
import os, glob

# === Target folders ===
COMFY = "/content/ComfyUI"
DIFF  = f"{COMFY}/models/diffusion_models"
LORA  = f"{COMFY}/models/loras"
for d in ["diffusion_models", "loras", "text_encoders", "vae"]:
    os.makedirs(f"{COMFY}/models/{d}", exist_ok=True)

# <<< imageToVideo.ipynb cell 9'daki fetch() fonksiyonunu buraya BİREBİR kopyala (docstring dahil) >>>
# def fetch(url, target_dir, filename, label, *, parallel, headers=None): ...

# Civitai auth: session cookie ONLY. A ?token= API key authenticates the request as that key's
# account -> creator-gated assets answer 401.
# Host = civitai.RED: the cookie is same-origin there. Sending it to .com is cross-domain and
# returns the login+turnstile page instead of the file.
def civitai_url(version_id):
    return f"https://civitai.red/api/download/models/{version_id}"

def cookie_header():
    return f"Cookie: __Secure-civ-token={COOKIE_VALUE}"

def civitai_probe(version_id, label):
    """Fail-fast: range-download the first 1KB to verify gated access BEFORE ~27GiB of checkpoints.
    On non-2xx or a login wall, surface Civitai's ACTUAL response body -- no hardcoded guesses."""
    out = "/content/_probe.bin"
    code = (run(["curl", "-sL", "--max-time", "60", "-r", "0-1023",
                 "-H", cookie_header(), "-w", "%{http_code}", "-o", out,
                 civitai_url(version_id)], f"probe {label}") or "").strip()[-3:]
    body = b""
    if os.path.exists(out):
        with open(out, "rb") as f:
            body = f.read(512)
        os.remove(out)
    # success = 2xx AND the body is real binary (safetensors), not an HTML/JSON error page
    if code.startswith("2") and not body.startswith(b"<") and not body.startswith(b'{"'):
        log(f"{label}: erişim OK", "OK")
        return
    raise RuntimeError(f"❌ {label}: HTTP {code} — Civitai yanıtı: "
                       f"{body.decode('utf-8', 'replace').strip() or '(boş gövde — binary değil)'}")

# === HuggingFace (aria2c) ===
WAN22 = "https://huggingface.co/Comfy-Org/Wan_2.2_ComfyUI_Repackaged/resolve/main/split_files"
WAN21 = "https://huggingface.co/Comfy-Org/Wan_2.1_ComfyUI_repackaged/resolve/main/split_files"

HF_MODELS = [
    # (url, target_dir, filename, label)
    (f"{WAN22}/loras/wan2.2_t2v_lightx2v_4steps_lora_v1.1_high_noise.safetensors", LORA, "wan2.2_t2v_lightx2v_4steps_lora_v1.1_high_noise.safetensors", "Lightx2v T2V HIGH"),
    (f"{WAN22}/loras/wan2.2_t2v_lightx2v_4steps_lora_v1.1_low_noise.safetensors",  LORA, "wan2.2_t2v_lightx2v_4steps_lora_v1.1_low_noise.safetensors",  "Lightx2v T2V LOW"),
    # VAE: the graph's VAELoader asks for 'Wan2_1_VAE_fp32.safetensors', so land the base under that name
    (f"{WAN21}/vae/wan_2.1_vae.safetensors",                          f"{COMFY}/models/vae",           "Wan2_1_VAE_fp32.safetensors",            "Wan2.1 VAE"),
    (f"{WAN21}/text_encoders/umt5_xxl_fp8_e4m3fn_scaled.safetensors", f"{COMFY}/models/text_encoders", "umt5_xxl_fp8_e4m3fn_scaled.safetensors", "UMT5-XXL"),
]

# === Civitai gated ===
# Civitai serves these as smoothMixWan2214BI2V_t2v*V30.safetensors; UNETLoader 37/56 ask for
# SmoothMix_T2V_*_v3.safetensors, so they land under the name the graph expects.
# NSFW LoRAs are trained for I2V-A14B (no T2V version exists) -> bypass them first if output is bad.
CIVITAI_MODELS = [
    # (version_id, target_dir, filename, label)
    (2768924, DIFF, "SmoothMix_T2V_High_v3.safetensors", "SmoothMix T2V v3 HIGH"),
    (2768944, DIFF, "SmoothMix_T2V_Low_v3.safetensors",  "SmoothMix T2V v3 LOW"),
    (2073605, LORA, "WAN_General_NSFW_HIGH.safetensors", "WAN General NSFW HIGH"),
    (2083303, LORA, "WAN_General_NSFW_LOW.safetensors",  "WAN General NSFW LOW"),
]

# 1) Fail-fast: verify gated access before spending 40 minutes on downloads
log(f"Gated probe: {len(CIVITAI_MODELS)} asset")
for vid, d, fn, label in CIVITAI_MODELS:
    civitai_probe(vid, label)

# 2) HuggingFace (parallel=True -> aria2c)
for url, d, fn, label in HF_MODELS:
    fetch(url, d, fn, label, parallel=True)

# 3) Civitai (parallel=False -> single-connection curl; aria2c forwards the cookie to B2 and gets 403)
for vid, d, fn, label in CIVITAI_MODELS:
    fetch(civitai_url(vid), d, fn, label, parallel=False, headers=cookie_header())

# === Summary (reaching here means everything downloaded + validated) ===
print("\n📂 diffusion_models/")
for f in sorted(glob.glob(f"{DIFF}/*.safetensors")):
    print(f"   {human(os.path.getsize(f))}  {os.path.basename(f)}")
print("📂 loras/")
for f in sorted(glob.glob(f"{LORA}/*.safetensors")):
    print(f"   {human(os.path.getsize(f))}  {os.path.basename(f)}")
log("Tüm modeller indirildi ve doğrulandı", "OK")
```

- [ ] **Step 3: Eski/kırık Civitai deseni sızmadı mı**

**Kullanımı ara, bahsi değil.** Eski desenin adı, onu yasaklayan yorumlarda geçer (`__Secure-civ-token (NOT the old __Secure-civitai-token)`, `never a ?token= API key`) — tıpkı `NOTEBOOK-STANDARD.md`'de `--fail`'in tek geçtiği yerin onu yasaklayan cümle olması gibi. O yüzden çıplak isim değil, **çağrı biçimi** aranır:

```bash
cd "d:/code/github/internal-tools/collab-toolbox/video_experiments/wan22-smooth-t2v"
grep -c 'civitai\.com/api/download\|CIVITAI_API_KEY\|Cookie: __Secure-civitai-token' wan22-smooth-t2v.ipynb
grep -o '__Secure-civ-token\|civitai\.red/api/download' wan22-smooth-t2v.ipynb | sort -u
```
Beklenen: ilk sayım **0** (eski desen hiçbir yerde *kullanılmıyor*), ikincisi iki satır da hit verir. Sıfır değilse DUR — `imageToVideo`'nun kırık auth'u kopyalanmış.

- [ ] **Step 4: Commit**

```bash
cd "d:/code/github/internal-tools"
git add collab-toolbox/video_experiments/wan22-smooth-t2v/wan22-smooth-t2v.ipynb
git commit -m "feat(wan22-smooth-t2v): model downloads with a gated pre-flight probe

Probe first: 27GiB of checkpoints should not start before we know the cookie
opens the gate, and a failed probe prints Civitai's own response body.

Checkpoints land renamed -- Civitai serves smoothMixWan2214BI2V_t2vHighV30 while
UNETLoader 37/56 ask for SmoothMix_T2V_High_v3. Civitai goes through curl
(parallel=False) because aria2c forwards the cookie to B2 and earns a 403.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Notebook — başlat + cloudflared tüneli

**Files:**
- Modify: `collab-toolbox/video_experiments/wan22-smooth-t2v/wan22-smooth-t2v.ipynb` (hücre ekle)

**Interfaces:**
- Consumes: `log()`, `run()` (Task 4), `COMFY_PORT` (Task 4).
- Produces: `trycloudflare` UI linki; hücre ön planda bloklu kalır.

- [ ] **Step 1: Markdown hücresi**

```markdown
## 5) Başlat + cloudflared Tünel

ComfyUI arka planda başlar (90 sn içinde `/system_stats` cevap vermezse fail-loud), tünel linki basılır, sonra hücre **bilerek açık kalır**.
```

- [ ] **Step 2: Kod hücresi**

`ltx23-eros/ltx23-eros.ipynb` c6'dan uyarlanır (`banner()` yerine `log()`, çünkü bu notebook'ta `banner` yok):

```python
import subprocess, time, urllib.request, re, os

if not os.path.isfile("/content/cloudflared"):
    run(["wget", "-q", "-O", "/content/cloudflared",
         "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64"], "cloudflared")
    run(["chmod", "+x", "/content/cloudflared"], "chmod cloudflared")

# Re-run safety: kill any previous ComfyUI + tunnel before starting new ones
subprocess.run(["pkill", "-f", "main.py"], check=False)
subprocess.run(["pkill", "-f", "cloudflared"], check=False)
time.sleep(2)

# --enable-manager: on current ComfyUI the Manager is OFF without this flag. The workflow is loaded
# by hand, so missing nodes are likely -> Manager must stay reachable in the UI.
logf = open("/content/comfyui.log", "w")
subprocess.Popen(["python", "main.py", "--listen", "127.0.0.1", "--port", str(COMFY_PORT), "--enable-manager"],
                 cwd="/content/ComfyUI", stdout=logf, stderr=subprocess.STDOUT)
ok = False
for i in range(45):
    time.sleep(2)
    try:
        urllib.request.urlopen(f"http://127.0.0.1:{COMFY_PORT}/system_stats", timeout=2)
        ok = True
        break
    except Exception:
        pass
if not ok:
    print("".join(open("/content/comfyui.log").readlines()[-30:]))
    raise RuntimeError("❌ ComfyUI 90 sn içinde başlamadı")
log(f"ComfyUI ayakta ({(i+1)*2}s)", "OK")

# cloudflared output goes to a file, not a pipe: an unread pipe fills up and blocks the process.
tunlog = "/content/cloudflared.log"
subprocess.Popen(["/content/cloudflared", "tunnel", "--url", f"http://127.0.0.1:{COMFY_PORT}"],
                 stdout=open(tunlog, "w"), stderr=subprocess.STDOUT)
link = None
for _ in range(30):
    time.sleep(1)
    m = re.search(r"https://[-\w.]+trycloudflare\.com", open(tunlog).read()) if os.path.exists(tunlog) else None
    if m:
        link = m.group(0)
        break
if not link:
    print(open(tunlog).read()[-1000:] if os.path.exists(tunlog) else "(cloudflared log yok)")
    raise RuntimeError("❌ cloudflared linki 30 sn içinde alınamadı")

print(f"\n🔗 ComfyUI linki: {link}\n")
print("⬆️ Linke gir → workflow.json'u yükle → 'Choose Your Workflow' → TEXT2VIDEO'yu aç")
print("⚠️ Power Lora Loader 109/110'a lightx2v LoRA'larını EKLE (6 step/cfg 1 distill LoRA ister) → prompt yaz → Run")
print("   Eksik node olursa: Manager → Install Missing Custom Nodes → Restart\n")

# === Keep the cell OPEN (critical) ===
# ComfyUI + tunnel run in the background. If this cell ENDS, Colab can treat the runtime as idle
# and disconnect -> ComfyUI + link die. Streaming the log keeps the cell in the foreground.
# To stop: interrupt this cell (■) or Runtime -> Disconnect.
print("📡 ComfyUI çalışıyor — BU HÜCREYİ KAPATMA. Canlı log:\n")
try:
    subprocess.run(["tail", "-n", "+1", "-f", "/content/comfyui.log"])
except KeyboardInterrupt:
    log("Hücre durduruldu — ComfyUI hâlâ arka planda çalışıyor (yeni link için bu hücreyi tekrar çalıştır).", "WARN")
```

- [ ] **Step 3: Notebook bütünlüğü**

```bash
cd "d:/code/github/internal-tools/collab-toolbox/video_experiments/wan22-smooth-t2v"
python -c "import json; nb=json.load(open('wan22-smooth-t2v.ipynb')); print('cells:', len(nb['cells'])); print([c['cell_type'] for c in nb['cells']])"
grep -c 'enable-manager' wan22-smooth-t2v.ipynb
```
Beklenen: `cells: 10` (5 markdown + 5 kod, sırayla md/code alternating), `enable-manager` sayımı ≥1.

- [ ] **Step 4: Commit**

```bash
cd "d:/code/github/internal-tools"
git add collab-toolbox/video_experiments/wan22-smooth-t2v/wan22-smooth-t2v.ipynb
git commit -m "feat(wan22-smooth-t2v): launch ComfyUI behind a cloudflared tunnel

--enable-manager stays on: the workflow is hand-loaded, so missing nodes are
likely and Manager has to be reachable. The cell blocks on tail -f on purpose --
if it ends, Colab can call the runtime idle and kill the tunnel.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: Colab'da uçtan uca doğrulama

**Files:**
- Modify: `collab-toolbox/video_experiments/deneme-listesi.md` (sonucu yaz)
- Modify: `collab-toolbox/video_experiments/wan22-smooth-t2v/indirilecekler.md` (flownet.pkl sonucu)

**Interfaces:**
- Consumes: Task 1-7'nin tamamı.

**Bu task kullanıcı tarafından elle yapılır** — Colab A100 gerekir, lokalde çalıştırılamaz. Kabul kriteri: **UI'da TEXT2VIDEO ile bir video üretilir.**

- [ ] **Step 1: Notebook'u Colab'da çalıştır**

Colab → Upload notebook → `wan22-smooth-t2v.ipynb` → Runtime → Change runtime type → **A100 GPU** → Run all.
Beklenen: hata yok; gated probe 4 asset için "erişim OK"; ~37 GiB iner; `trycloudflare` linki basılır.
Probe HTTP 401/403 verirse → cookie süresi dolmuş: `civitai.red`'de yeniden login, CONFIG'i güncelle.

- [ ] **Step 2: Workflow'u yükle — missing node var mı**

Linke gir → `workflow.json`'u sürükle.
Beklenen: **"missing node" hatası YOK.** Varsa → Manager → Install Missing Custom Nodes → eksik paketi `indirilecekler.md` §1'e ve Task 5'in `CUSTOM_NODES` listesine ekle.

- [ ] **Step 3: Model dropdown'ları — rename doğru çalıştı mı**

UNETLoader 37/56'ya tıkla.
Beklenen: `SmoothMix_T2V_High_v3.safetensors` / `SmoothMix_T2V_Low_v3.safetensors` listede.
Yoksa → `fetch()`'in `filename` argümanı yanlış; `indirilecekler.md` §2'deki adlarla karşılaştır.

- [ ] **Step 4: TEXT2VIDEO'yu aç + LoRA'ları ekle + üret**

"Choose Your Workflow" → TEXT2VIDEO. Power Lora Loader **109**'a `wan2.2_t2v_lightx2v_4steps_lora_v1.1_high_noise`, **110**'a `..._low_noise` ekle (strength 1.0). PromptGenerator 230:229'a prompt yaz. Run.
Beklenen: 480×720, 5 sn, 32 fps mp4. `flownet.pkl` otomatik iniyor mu — konsol log'unda gör.

- [ ] **Step 5: flownet.pkl sonucunu manifeste yaz**

Otomatik indiyse `indirilecekler.md` §5'teki ⚠️'yi ✅ yap. İnmediyse Task 6'nın model listesine ekle ve ⚠️'yi güncelle. **Tahmin etme — run'da ne olduysa onu yaz** (drift yasağı).

- [ ] **Step 6: NSFW LoRA'yı dene (opsiyonel)**

Power Lora Loader'a `WAN_General_NSFW_HIGH/LOW` ekle → Run. Çıktı bozulursa bypass et ve `deneme-listesi.md` notuna yaz — I2V-A14B için eğitilmiş olması beklenen sebep.

- [ ] **Step 7: Sonucu backlog'a yaz + commit**

`deneme-listesi.md`'deki maddeyi güncelle: `- [ ]` → `- [x]`, `Durum: bekliyor` → gerçek sonuç, `Not: —` → gözlem (T2V kalitesi I2V'ye göre nasıl, LoRA'lar tuttu mu).

```bash
cd "d:/code/github/internal-tools"
git add collab-toolbox/video_experiments/deneme-listesi.md collab-toolbox/video_experiments/wan22-smooth-t2v/indirilecekler.md
git commit -m "docs(wan22-smooth-t2v): record the Colab run result

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

## Self-review notu

- **Spec kapsamı:** spec'in her bölümü bir task'a bağlı — mimari→T1, manifest→T2, kullanım+model ekleme→T3, notebook→T4-7, doğrulama→T8. Spec'teki 5 riskin hepsi karşılandı: LoRA uyarısı (T3 Step 1 + T7 Step 2 print), NSFW I2V (T2/T3/T6 yorumu + T8 Step 6), flownet.pkl (T2 §5 ⚠️ + T8 Step 5), VAE rename (T2 §3 + T6 inline yorum), boş prompt (T3 Kullanım §7).
- **Spec'ten sapma:** spec "notebook ltx23-eros'tan kopyalanır" diyordu; plan hibrit yapıyor (helpers+fetch imageToVideo'dan). Gerekçe Global Constraints'te — `is_valid_safetensors` 13.5 GiB'lık kesik indirmeyi yakalamaz. Spec bu yönde güncellenmeli.
- **Tip tutarlılığı:** `fetch(..., *, parallel, headers)`, `check_safetensors(path) -> (str, str)`, `run(cmd, label, cwd, timeout) -> stdout`, `civitai_probe(version_id, label) -> None` — Task 4'te tanımlandığı gibi Task 5/6/7'de kullanılıyor. `ltx23-eros`'un `curl_first`'ü **kullanılmıyor**: `parallel=False` zaten "yalnız curl" demek, B2 sorununu aynı şekilde çözer.
