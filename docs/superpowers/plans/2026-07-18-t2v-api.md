# WAN 2.2 T2V — API modu Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb` — CONFIG'e prompt yaz, çalıştır, video Drive'a insin. UI yok, tünel yok, batch yok.

**Architecture:** Notebook ComfyUI'yi arka planda API olarak açar, Drive'daki `workflow_api.json`'u okur, prompt+seed'i node'lara basar, `/prompt`'a POST eder, `/history`'yi yoklar, videoyu `/view`'dan alıp Drive'a yazar. Kurulum hücreleri (helpers / custom node / model / başlatma) `manual.ipynb`'den, API katmanı `loop_maker/comfy_ui.ipynb`'den gelir.

**Tech Stack:** Google Colab (A100), ComfyUI HTTP API, `requests`, Google Drive, aria2c/curl, Civitai gated download (cookie), HuggingFace.

## Global Constraints

- **Branch: `feat/wan22-smooth-t2v-api`** (açık, HEAD `938490d`). Yeni branch açma.
- **`--amend` KULLANMA.** Aynı çalışma dizininde ikinci bir Claude oturumu commit atabiliyor; amend HEAD'i yeniden yazar ve onların commit'ini ezer (18 Tem'de bir kez oldu). Mesaj yanlışsa düzeltme commit'i at.
- **`git add`'e her zaman açık dosya yolu ver** — asla `git add .` / `git add -A`.
- **`docs/superpowers/` artık commit ediliyor** (kural 18 Tem'de tersine döndü). Plan ve spec repoya girer.
- **Python çalıştırma.** Dosya incelemek için Grep/Read; JSON doğrulaması için PowerShell `ConvertFrom-Json`.
- **Dil** (kök `CLAUDE.md`): markdown hücreleri + runtime çıktısı (`print`/`log`/`assert`/`RuntimeError`) **Türkçe**; kod yorumları + docstring'ler **İngilizce**.
- **Referans `loop_maker/comfy_ui.ipynb`.** `imageToVideo.ipynb`'den API kodu kopyalanmaz — `wait()`'inde timeout yok, Drive'ı geç mount ediyor, Civitai auth'u kırık, ComfyUI'nin disk kopyasını temizlemiyor.
- **`workflow_api.json` değiştirilmez.** Repodaki kopya referans; notebook Drive'dakini okur.
- **lightx2v indirilmez.** SmoothMix T2V v3'te baked in; ikinci kez uygulamak çıktıyı bozuyor.
- **Fail-loud:** her hata `RuntimeError` + servisin **ham** çıktısı. Sebep uydurma.

---

### Task 1: Notebook iskeleti — başlık + CONFIG + helpers

**Files:**
- Create: `collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb`
- Read (kaynak): `collab-toolbox/video_generator/wan22-smooth-t2v/manual.ipynb` (hücre 2, 4), `collab-toolbox/loop_maker/comfy_ui.ipynb` (hücre 2, 4)

**Interfaces:**
- Produces: `PROMPT`, `DRIVE_ROOT`, `WORKFLOW_PATH`, `OUTPUT_DIR`, `COMFYUI_URL`, `COMFY_ROOT`, `COMFY_OUTPUT_DIR`, `COMFY_LOG`, `TIMEOUT_PER_RENDER`, `POLL_INTERVAL`, `COOKIE_VALUE`, `COMFY_PORT`; helpers `log(msg, level="INFO")`, `human(b)`, `head_text(path, limit=4000)`, `run(cmd, label, cwd=None, timeout=3600) -> stdout`, `check_safetensors(path) -> (str, str)`, `describe_comfy_error(status) -> (text, tb, is_infra)`. Task 2-5 bunları kullanır.

- [ ] **Step 1: Başlık hücresi (markdown)**

```markdown
# WAN 2.2 Smooth T2V — API modu (Colab)

Prompt yaz → çalıştır → video Drive'a düşer. ComfyUI arka planda **API** olarak çalışır; UI açılmaz.

**Input:** `PROMPT` (aşağıdaki CONFIG) + `workflow_api.json` (Drive) · **Output:** `MyDrive/TextToVideo/output/YYYYAAGG_SSDD.mp4`

```
TextToVideo/
├── workflow_api.json   ← ComfyUI → Export (API) ile kaydedilmiş graf
└── output/             ← üretilen videolar, zaman damgalı
```

Sıra:
1. **CONFIG** — Drive mount + prompt
2. **Ortak Yardımcılar** — log + fail-loud run + model doğrulama
3. **ComfyUI + custom node'lar** (16)
4. **Modeller** — önce gated probe, sonra indir (~33.5 GiB)
5. **ComfyUI'yi başlat** (arka planda, API)
6. **Üret** — prompt'u bas, kuyruğa at, videoyu Drive'a yaz

> **Yeni prompt için:** CONFIG'deki `PROMPT`'u değiştir ve **sadece 6. hücreyi** tekrar çalıştır. Modeller inmiş, ComfyUI ayakta — yaklaşık 1 dakika.

> **LoRA yok, olması da gerekmiyor.** SmoothMix T2V v3'te lightx2v checkpoint'e merge edilmiş (`light2xv baked in`); ayrıca yüklemek iki kez uygular ve çıktıyı bozar. Graf 6 step / cfg 1 ile LoRA'sız çalışır.
```

- [ ] **Step 2: CONFIG markdown hücresi**

```markdown
## 1) CONFIG

Google Drive **burada** mount edilir: auth istemi ilk saniyede çıksın, 33 GiB'lık model indirmesinin ortasında seni beklemesin.

Doldurulacak tek yer `PROMPT`. Cookie'nin süresi dolmuşsa (~30 gün) `civitai.red`'den yenile.
```

- [ ] **Step 3: CONFIG kod hücresi**

`COOKIE_VALUE`'yu `manual.ipynb` hücre 2'den **birebir kopyala**.

```python
# === Google Drive — en başta mount edilir ===
# Auth istemi ilk saniyede çıksın: 33 GiB'lık model indirmesinin ortasında beklemesin.
from google.colab import drive
drive.mount('/content/drive')

# === Prompt — değiştirip 6. hücreyi tekrar çalıştır ===
PROMPT = ""

# === Drive ===
DRIVE_ROOT        = "/content/drive/MyDrive/TextToVideo"
WORKFLOW_FILENAME = "workflow_api.json"      # DRIVE_ROOT altında, API format

# === Civitai login-gated download ===
# civitai.red -> log in -> F12 -> Application -> Cookies -> __Secure-civ-token değerini yapıştır
# (çift tıkla -> Ctrl+A -> Ctrl+C; tek tık hücreyi kırpar ve `assert len>200` yine geçer).
# NOTE: auth moved to auth.civitai.com -> the cookie NAME is __Secure-civ-token (NOT the old
#   __Secure-civitai-token) and the value is a short ES256 JWT (~420 chars), not the old long JWE.
# Cookie only; never a ?token= API key -> gated assets answer 401.
COOKIE_VALUE = "<manual.ipynb hücre 2'deki COOKIE_VALUE ile aynı>"

# === Render ===
TIMEOUT_PER_RENDER = 30 * 60   # saniye — bu süre içinde bitmezse fail-loud
POLL_INTERVAL      = 5         # saniye — /history yoklama aralığı

# === Derived paths ===
COMFY_PORT       = 8188
COMFYUI_URL      = f"http://127.0.0.1:{COMFY_PORT}"
WORKFLOW_PATH    = f"{DRIVE_ROOT}/{WORKFLOW_FILENAME}"
OUTPUT_DIR       = f"{DRIVE_ROOT}/output"

COMFY_ROOT       = "/content/ComfyUI"
COMFY_OUTPUT_DIR = f"{COMFY_ROOT}/output"
COMFY_LOG        = "/content/comfyui.log"

import os
os.makedirs(OUTPUT_DIR, exist_ok=True)
assert len(COOKIE_VALUE) > 200, "❌ COOKIE_VALUE boş/çok kısa — civitai.red'den __Secure-civ-token (ES256 JWT) yapıştır"
assert os.path.exists(WORKFLOW_PATH), f"❌ Workflow yok: {WORKFLOW_PATH} — ComfyUI'de 'Workflow → Export (API)' ile kaydedip Drive'a koy"
assert PROMPT.strip(), "❌ PROMPT boş — yukarıya prompt'unu yaz"

print(f"✓ Drive: {DRIVE_ROOT}")
print(f"✓ Cookie: {len(COOKIE_VALUE)} char  |  Timeout: {TIMEOUT_PER_RENDER // 60} dk")
print(f"✓ Prompt: {PROMPT[:70]}{'…' if len(PROMPT) > 70 else ''}")
print("=== GPU ===")
!nvidia-smi --query-gpu=name,memory.total,driver_version --format=csv,noheader
```

- [ ] **Step 4: Helpers markdown hücresi**

```markdown
## 2) Ortak Yardımcılar

`log` + fail-loud `run` + model doğrulama + ComfyUI hata çözümleyici.

Model doğrulaması **ağa soru sormaz**: safetensors header'ındaki `data_offsets` beklenen boyutu verir. HEAD/`Content-Length` kullanılmaz — HF'in Xet CDN'i imzalı URL'de HEAD'e 403 döner ve o hata gövdesi "dosya boyutu" sanılır.
```

- [ ] **Step 5: Helpers kod hücresi**

`manual.ipynb` hücre 4'ü **birebir kopyala** (`log`, `human`, `head_text`, `run`, `check_safetensors` — docstring'leriyle), sonuna `loop_maker` hücre 4'ten `describe_comfy_error`'ı **birebir** ekle:

```python
def describe_comfy_error(status):
    """Raw execution_error from ComfyUI's history status -> (text, traceback, is_infra).

    is_infra = the failing node is a model loader, so the model is broken or missing and every
    render would hit the identical error.
    """
    for entry in status.get("messages", []):
        if not (isinstance(entry, (list, tuple)) and len(entry) == 2):
            continue
        kind, data = entry
        if kind != "execution_error" or not isinstance(data, dict):
            continue
        node_type = str(data.get("node_type", "?"))
        text = (f"node {data.get('node_id')} ({node_type})\n"
                f"{data.get('exception_type')}: {str(data.get('exception_message', '')).strip()}\n"
                f"inputs: {data.get('current_inputs')}")
        tb = "".join(data.get("traceback", []) or [])
        return text, tb, node_type.lower().endswith("loader")
    return f"status: {json.dumps(status, ensure_ascii=False)}", "", False
```

Son satır:

```python
print("✓ Ortak yardımcılar hazır (log, run, human, head_text, check_safetensors, describe_comfy_error)")
```

- [ ] **Step 6: Notebook JSON geçerli mi**

```powershell
Get-Content collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb -Raw | ConvertFrom-Json | Select-Object -ExpandProperty cells | Measure-Object | Select-Object -ExpandProperty Count
```
Beklenen: `4`, hata yok.

- [ ] **Step 7: Commit**

```bash
git add collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb
git commit -m "feat(wan22-smooth-t2v): API-mode notebook config and helpers

Drive mounts on the first line of CONFIG, not three cells later: the auth prompt
has to appear in the first second rather than halfway through a 33 GiB download
(NOTEBOOK-STANDARD section 1, which imageToVideo gets wrong).

describe_comfy_error comes from loop_maker so a failed render prints ComfyUI's
own node id, exception and traceback instead of a summary of them.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 2: ComfyUI + custom node'lar

**Files:**
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb` (hücre ekle)

**Interfaces:**
- Consumes: `log()`, `run()` (Task 1).
- Produces: `/content/ComfyUI` + 16 custom node kurulu.

- [ ] **Step 1: Markdown hücresi**

```markdown
## 3) ComfyUI + Custom Node'lar (16)

Liste `manual.ipynb` ile birebir aynı — API grafiği aynı node'ları kullanıyor. Biri başarısız olursa hücre `RuntimeError` ile durur (fail-loud); eksik node ileride "node not found" olarak karşımıza çıkmaz.
```

- [ ] **Step 2: Kod hücresi**

`manual.ipynb` hücre 6'yı **birebir kopyala** (16 satırlık `CUSTOM_NODES` listesi + döngü, inline yorumlarıyla).

- [ ] **Step 3: Liste eksiksiz mi**

```powershell
Select-String -Path collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb -Pattern 'https://github\.com/[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+\.git' -AllMatches | ForEach-Object { $_.Matches.Value } | Sort-Object -Unique | Measure-Object | Select-Object -ExpandProperty Count
```
Beklenen: **17** (16 custom node + ComfyUI'nin kendisi).

- [ ] **Step 4: Commit**

```bash
git add collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb
git commit -m "feat(wan22-smooth-t2v): ComfyUI and the 16 custom nodes

Copied verbatim from the UI notebook in the same folder. The API graph is an
export of that graph's TEXT2VIDEO group, so it needs the same node classes --
PromptGenerator, RIFEInterpolation, VHS_VideoCombine, mxSlider2D and the rest.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 3: Modeller

**Files:**
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb` (hücre ekle)

**Interfaces:**
- Consumes: `log()`, `run()`, `human()`, `head_text()`, `check_safetensors()`, `COOKIE_VALUE`, `COMFY_ROOT` (Task 1).
- Produces: `fetch(url, target_dir, filename, label, *, parallel, headers=None)`, `civitai_url(version_id)`, `cookie_header()`, `civitai_probe(version_id, label)`; `/content/ComfyUI/models/**` dolu.

- [ ] **Step 1: Markdown hücresi**

```markdown
## 4) Modeller — önce gated probe, sonra indir (~33.5 GiB)

Gated erişim **ağır indirmeden önce** doğrulanır (ilk 1 KB): cookie ölmüşse 27 GiB'lık checkpoint indirmeye başlamadan, Civitai'nin **gerçek yanıtıyla** durur.

Bozuk/eksik inen dosyada hücre `RuntimeError` ile durur; bozuk dosya **silinmez**, inceleme için diskte kalır.

Civitai checkpoint'leri `smoothMixWan2214BI2V_t2v*V30.safetensors` adıyla gelir; grafiğin UNETLoader **37**/**56**'sı `SmoothMix_T2V_*_v3.safetensors` beklediği için o adla iner. VAE'de de aynı durum: `wan_2.1_vae.safetensors` → `Wan2_1_VAE_fp32.safetensors`.

**lightx2v inmiyor:** T2V v3'te checkpoint'e merge edilmiş, ayrıca yüklemek iki kez uygular.
```

- [ ] **Step 2: Kod hücresi**

`manual.ipynb` hücre 8'i kopyala, **iki farkla**: `HF_MODELS`'tan lightx2v satırlarını, `CIVITAI_MODELS`'tan NSFW LoRA satırlarını çıkar.

```python
import os, glob

# === Target folders ===
COMFY = COMFY_ROOT
DIFF  = f"{COMFY}/models/diffusion_models"
LORA  = f"{COMFY}/models/loras"
for d in ["diffusion_models", "loras", "text_encoders", "vae"]:
    os.makedirs(f"{COMFY}/models/{d}", exist_ok=True)

# <<< manual.ipynb hücre 8'deki fetch() fonksiyonunu buraya BİREBİR kopyala >>>
# <<< civitai_url() / cookie_header() / civitai_probe() de birebir kopyalanır >>>

# === HuggingFace models (aria2c) ===
WAN21 = "https://huggingface.co/Comfy-Org/Wan_2.1_ComfyUI_repackaged/resolve/main/split_files"

HF_MODELS = [
    # (url, target_dir, filename, label)
    # VAELoader 39 asks for 'Wan2_1_VAE_fp32.safetensors', so land the base under that name
    (f"{WAN21}/vae/wan_2.1_vae.safetensors",                          f"{COMFY}/models/vae",           "Wan2_1_VAE_fp32.safetensors",            "Wan2.1 VAE"),
    (f"{WAN21}/text_encoders/umt5_xxl_fp8_e4m3fn_scaled.safetensors", f"{COMFY}/models/text_encoders", "umt5_xxl_fp8_e4m3fn_scaled.safetensors", "UMT5-XXL"),
]

# === Civitai gated models (curl + login cookie) ===
# Civitai serves these as smoothMixWan2214BI2V_t2v*V30.safetensors; UNETLoader 37/56 ask for
# SmoothMix_T2V_*_v3.safetensors, so they land under the name the graph expects.
# No lightx2v here: T2V v3 has the distill LoRA merged into the checkpoint, and the exported
# graph runs 6 steps at cfg 1 with every Power Lora Loader empty.
CIVITAI_MODELS = [
    # (version_id, target_dir, filename, label)
    (2768924, DIFF, "SmoothMix_T2V_High_v3.safetensors", "SmoothMix T2V v3 HIGH"),
    (2768944, DIFF, "SmoothMix_T2V_Low_v3.safetensors",  "SmoothMix T2V v3 LOW"),
]

# 1) Fail-fast: verify gated access before spending half an hour on downloads
log(f"Gated probe: {len(CIVITAI_MODELS)} asset")
for vid, d, fn, label in CIVITAI_MODELS:
    civitai_probe(vid, label)

# 2) HuggingFace
for url, d, fn, label in HF_MODELS:
    fetch(url, d, fn, label, parallel=True)

# 3) Civitai — parallel=False: aria2c forwards the cookie to the B2 store on redirect and gets 403,
#    curl drops it cross-host and gets through.
for vid, d, fn, label in CIVITAI_MODELS:
    fetch(civitai_url(vid), d, fn, label, parallel=False, headers=cookie_header())

# === Summary (reaching here means everything downloaded + validated) ===
print("\n📂 diffusion_models/")
for f in sorted(glob.glob(f"{DIFF}/*.safetensors")):
    print(f"   {human(os.path.getsize(f))}  {os.path.basename(f)}")
log("Tüm modeller indirildi ve doğrulandı", "OK")
```

- [ ] **Step 3: lightx2v gerçekten yok mu**

**Kullanımı ara, bahsi değil.** `lightx2v` kelimesi markdown'da bilerek geçiyor ("inmiyor, çünkü merged"), o yüzden çıplak isim değil **indirme satırının biçimi** aranır:

```powershell
Select-String -Path collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb -Pattern 'loras/wan2\.2_t2v_lightx2v|WAN_General_NSFW' | Measure-Object | Select-Object -ExpandProperty Count
```
Beklenen: **0**. Sıfır değilse DUR — UI notebook'unun model listesi olduğu gibi kopyalanmış.

- [ ] **Step 4: Commit**

```bash
git add collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb
git commit -m "feat(wan22-smooth-t2v): model downloads without the distill LoRA

Four files instead of the UI notebook's eight. lightx2v is gone because SmoothMix
T2V v3 ships it merged into the checkpoint -- loading it again applies it twice,
which is what made hand-tuned runs look worse than the LoRA-free ones. The
exported graph corroborates it: 6 steps at cfg 1 with every Power Lora Loader
empty only works when the distill is already in the weights.

The NSFW LoRAs are gone too. They were there for UI experimentation and the API
graph has nowhere to load them.

Probe still runs first: 27 GiB should not start downloading before we know the
cookie opens the gate.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 4: ComfyUI'yi başlat (API modu)

**Files:**
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb` (hücre ekle)

**Interfaces:**
- Consumes: `log()`, `COMFY_ROOT`, `COMFY_PORT`, `COMFYUI_URL`, `COMFY_LOG` (Task 1).
- Produces: 127.0.0.1:8188'de çalışan ComfyUI. Hücre **bloklamaz** — biter ve sonraki hücreye geçilir.

- [ ] **Step 1: Markdown hücresi**

```markdown
## 5) ComfyUI'yi Başlat (Arka Planda)

ComfyUI subprocess olarak başlar, üretim localhost API'sine konuşur. **90 sn içinde hazır olmazsa** hücre log'un son 30 satırını basıp durur — sonraki hücre ölü sunucuya çalışmasın.

Tünel yok: UI'a girilmiyor. Grafiği değiştirmek istersen `manual.ipynb`'yi çalıştır, UI'da düzenle, **Export (API)** ile Drive'daki `workflow_api.json`'un üzerine yaz.

Bu hücre **bloklamaz**. Colab'ın runtime'ı idle sayması, sonraki hücre çalıştığı sürece sorun değil.
```

- [ ] **Step 2: Kod hücresi**

`loop_maker` hücre 12'den uyarlanır — tünel bloğu **alınmaz**:

```python
import subprocess, time, os, urllib.request

# Re-run safety: kill the previous instance before starting a new one
os.system("pkill -f 'python main.py' 2>/dev/null")
time.sleep(2)

# === Start in background (logs to file) ===
comfy_log = open(COMFY_LOG, "w")
proc = subprocess.Popen(
    ["python", "main.py", "--listen", "127.0.0.1", "--port", str(COMFY_PORT)],
    cwd=COMFY_ROOT, stdout=comfy_log, stderr=subprocess.STDOUT,
)
log(f"ComfyUI başlatıldı (PID {proc.pid}), log: {COMFY_LOG}")

# === Ready? max 90s — otherwise fail-loud with the server's own log ===
for i in range(45):
    time.sleep(2)
    try:
        urllib.request.urlopen(f"{COMFYUI_URL}/system_stats", timeout=2)
        log(f"ComfyUI hazır ({(i + 1) * 2}s)", "OK")
        break
    except Exception:
        pass
else:
    with open(COMFY_LOG) as f:
        print("".join(f.readlines()[-30:]))
    raise RuntimeError("❌ ComfyUI 90 sn içinde başlamadı — yukarıdaki log'a bak")
```

**`--enable-manager` YOK.** UI'a girilmediği için Manager'a gerek yok; eksik node zaten Task 2'de fail-loud yakalanır.

- [ ] **Step 3: Commit**

```bash
git add collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb
git commit -m "feat(wan22-smooth-t2v): start ComfyUI in API mode

No tunnel and no --enable-manager: nothing opens the UI here, and a missing node
already fails loudly during install. The cell returns instead of blocking on
tail -f, because the render cell below it is what keeps the runtime busy.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 5: Üretim hücresi

**Files:**
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb` (hücre ekle)

**Interfaces:**
- Consumes: `log()`, `describe_comfy_error()`, `PROMPT`, `WORKFLOW_PATH`, `OUTPUT_DIR`, `COMFYUI_URL`, `COMFY_OUTPUT_DIR`, `TIMEOUT_PER_RENDER`, `POLL_INTERVAL` (Task 1).
- Produces: `ComfyExecutionError`, `load_workflow(path)`, `set_prompt(workflow, prompt)`, `set_seed(workflow, seed)`, `ComfyClient`, `generate(prompt) -> save_path`. **Tekrar çalıştırılabilir hücre budur.**

**Neden bu tasarım:** kullanıcı tek video üretiyor, bu yüzden `loop_maker`'ın batch mantığı (`PLAN`, passthrough, ardışık hata sayacı, resume) **taşınmaz** — YAGNI. Timeout'lu `wait()` ve ham hata basımı taşınır, çünkü ikisi de tek render'da da gerekli.

- [ ] **Step 1: Markdown hücresi**

```markdown
## 6) Üret

**Tekrar çalıştırılacak hücre bu.** Yeni prompt için CONFIG'deki `PROMPT`'u değiştir ve buraya dön — kurulumu tekrarlamaya gerek yok.

Seed her çalıştırmada rastgele ve **ekrana basılır**: beğendiğin çıktıyı tekrar üretmek istersen `generate(PROMPT, seed=...)` ile aynı seed'i verebilirsin.

Çıktı `output/YYYYAAGG_SSDD.mp4` olarak Drive'a yazılır; art arda çalıştırmalar birbirini ezmez.
```

- [ ] **Step 2: Kod hücresi**

```python
import copy, json, os, random, time, uuid, requests

class ComfyExecutionError(RuntimeError):
    """A prompt failed inside ComfyUI. Carries the raw error, plus whether it is infra-level.
    infra=True -> a model loader node failed, so the model is broken or missing."""
    def __init__(self, text, traceback_text, infra):
        super().__init__(text)
        self.text = text
        self.traceback_text = traceback_text
        self.infra = infra

# === Node ids (from workflow_api.json) ===
# Opaque strings, not numbers: "230:229" and "235:57" are subgraph-flattened ids.
PROMPT_NODE = "230:229"   # PromptGenerator, inputs.prompt -> CLIPTextEncode 90
SEED_NODE   = "82"        # Seed (rgthree) -> KSamplerAdvanced 235:57 noise_seed

# === Template I/O + patchers (SRP: one function injects one field) ===
def load_workflow(path):
    with open(path, encoding="utf-8") as f:
        wf = json.load(f)
    if "nodes" in wf:
        raise RuntimeError(
            "workflow_api.json UI formatında — ComfyUI'de 'Workflow → Export (API)' ile kaydet"
        )
    for node_id in (PROMPT_NODE, SEED_NODE):
        if node_id not in wf:
            raise RuntimeError(f"Workflow'da {node_id} node yok — graf değişmiş, node id'leri güncelle")
    return wf

def set_prompt(workflow, prompt):
    workflow[PROMPT_NODE]["inputs"]["prompt"] = prompt

def set_seed(workflow, seed):
    """Both seeds: the sampler's noise seed and PromptGenerator's own, so a rerun with the same
    seed reproduces the video even when the prompt uses wildcard syntax."""
    workflow[SEED_NODE]["inputs"]["seed"] = seed
    workflow[PROMPT_NODE]["inputs"]["seed"] = seed

# === ComfyUI HTTP client ===
class ComfyClient:
    def __init__(self, base_url):
        self.base = base_url.rstrip("/")
        self.client_id = str(uuid.uuid4())

    def submit(self, workflow):
        r = requests.post(f"{self.base}/prompt",
                          json={"prompt": workflow, "client_id": self.client_id}, timeout=30)
        if r.status_code >= 400:
            raise RuntimeError(f"POST /prompt -> HTTP {r.status_code}\n{r.text}")
        data = r.json()
        if data.get("node_errors"):
            raise RuntimeError("POST /prompt -> node_errors\n"
                               + json.dumps(data["node_errors"], indent=2, ensure_ascii=False))
        return data["prompt_id"]

    def wait(self, prompt_id, timeout):
        start = time.time()
        while True:
            if time.time() - start > timeout:
                raise TimeoutError(f"prompt {prompt_id}: {timeout}s içinde bitmedi")
            history = requests.get(f"{self.base}/history/{prompt_id}", timeout=30).json()
            if prompt_id in history:
                entry = history[prompt_id]
                status = entry.get("status", {})
                if status.get("status_str") == "error":
                    raise ComfyExecutionError(*describe_comfy_error(status))
                return entry
            time.sleep(POLL_INTERVAL)

    def save_output_video(self, history_entry, save_path):
        """Pull the produced video over /view — independent of ComfyUI's output subfolder layout.
        Returns the path ComfyUI used, so the caller can drop the local copy."""
        for node_output in history_entry.get("outputs", {}).values():
            for key in ("gifs", "videos", "images"):
                for item in node_output.get(key, []):
                    if not item.get("filename", "").lower().endswith((".mp4", ".webm", ".mov")):
                        continue
                    r = requests.get(f"{self.base}/view", timeout=300, params={
                        "filename":  item["filename"],
                        "subfolder": item.get("subfolder", ""),
                        "type":      item.get("type", "output"),
                    })
                    r.raise_for_status()
                    with open(save_path, "wb") as f:
                        f.write(r.content)
                    return os.path.join(item.get("subfolder", ""), item["filename"])
        raise RuntimeError("history'de video çıktısı yok:\n"
                           + json.dumps(history_entry.get("outputs", {}), indent=2, ensure_ascii=False))

# === One render, end to end ===
def generate(prompt, seed=None):
    """Prompt -> queue -> wait -> Drive. Returns the saved path."""
    seed = random.randint(0, 2**31 - 1) if seed is None else seed
    save_path = os.path.join(OUTPUT_DIR, f"{time.strftime('%Y%m%d_%H%M%S')}.mp4")

    workflow = load_workflow(WORKFLOW_PATH)
    set_prompt(workflow, prompt)
    set_seed(workflow, seed)

    client = ComfyClient(COMFYUI_URL)
    log(f"seed={seed}  |  {prompt[:60]}{'…' if len(prompt) > 60 else ''}")
    t0 = time.time()
    prompt_id = client.submit(workflow)
    log(f"kuyrukta: {prompt_id}")

    try:
        history = client.wait(prompt_id, TIMEOUT_PER_RENDER)
    except ComfyExecutionError as e:
        print(e.text)
        print(e.traceback_text)
        raise RuntimeError("Üretim başarısız — yukarıdaki ComfyUI hatasına bak") from None

    produced = client.save_output_video(history, save_path)
    # ComfyUI's own copy is dropped as well, so a long session does not fill the Colab disk.
    local_out = os.path.join(COMFY_OUTPUT_DIR, produced)
    if os.path.exists(local_out):
        os.remove(local_out)

    size_mb = os.path.getsize(save_path) / 1024**2
    log(f"bitti ({time.time() - t0:.0f}s, {size_mb:.1f} MB) → {save_path}", "OK")
    return save_path

video_path = generate(PROMPT)
```

- [ ] **Step 3: Notebook bütünlüğü**

```powershell
$nb = Get-Content collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb -Raw | ConvertFrom-Json
$nb.cells.Count
$nb.cells | ForEach-Object { $_.cell_type }
```
Beklenen: **13** hücre — tek başına duran başlık hücresi + (markdown, code) × 6. Sıra: `markdown, markdown, code, markdown, code, markdown, code, markdown, code, markdown, code, markdown, code`.

- [ ] **Step 4: Commit**

```bash
git add collab-toolbox/video_generator/wan22-smooth-t2v/api.ipynb
git commit -m "feat(wan22-smooth-t2v): the render cell

The re-runnable one: change PROMPT in CONFIG, run this cell, get another video.
Everything above it is setup that survives between renders.

Seed is drawn in python rather than left at the graph's -1. rgthree's Seed node
randomises -1 in its frontend widget, and there is no frontend in API mode, so
-1 risks producing the same video every run. It is printed, so a result worth
keeping can be reproduced with generate(PROMPT, seed=...).

Both seeds are set: the sampler's and PromptGenerator's own, since that node
expands wildcard syntax and would otherwise vary independently.

load_workflow rejects a UI-format file and checks the two node ids exist, so a
re-export that renumbers them fails here with a clear message rather than
silently rendering an unprompted video.

loop_maker's batch machinery is deliberately left out -- one render per run needs
no plan, no resume and no consecutive-failure counter. Its timeout and raw error
printing are kept, because both matter for a single render too.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 6: `instructions.md` — API modu bölümü

**Files:**
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/instructions.md`

**Interfaces:**
- Consumes: Task 1-5'in notebook'u.

- [ ] **Step 1: Dosyanın sonuna bölüm ekle**

```markdown
## API modu — UI'a girmeden üretmek

`api.ipynb` aynı grafiği ComfyUI'nin HTTP API'si üzerinden çalıştırır: prompt yaz, çalıştır, video Drive'a insin.

**Drive kurulumu (bir kez):**

```
MyDrive/TextToVideo/
├── workflow_api.json   ← bu klasördeki workflow_api.json'un kopyası
└── output/             ← otomatik oluşur
```

**Kullanım:**

1. Runtime → **A100 GPU**
2. `api.ipynb` → CONFIG'de `PROMPT`'u doldur → **Run all** (~33.5 GiB iner, ilk çalıştırma ~10 dk)
3. Video `output/YYYYAAGG_SSDD.mp4` olarak Drive'a düşer
4. **Yeni prompt:** `PROMPT`'u değiştir, **sadece 6. hücreyi** çalıştır (~1 dk)

**Grafiği değiştirmek:** `manual.ipynb`'yi çalıştır, UI'da düzenle, **Workflow → Export (API)** → Drive'daki `workflow_api.json`'un üzerine yaz. Notebook'a dokunma.

Notebook yalnızca `PROMPT` ve seed'i grafiğe basar; çözünürlük, süre, step/cfg, RIFE ayarları grafikte ne yazıyorsa odur.

**Seed:** her çalıştırmada rastgele ve ekrana basılır. Beğendiğin çıktıyı tekrarlamak için `generate(PROMPT, seed=123456)`.
```

- [ ] **Step 2: Commit**

```bash
git add collab-toolbox/video_generator/wan22-smooth-t2v/instructions.md
git commit -m "docs(wan22-smooth-t2v): document API mode

Says where the Drive folder goes, that only the render cell needs rerunning for
a new prompt, and that the graph is changed by re-exporting over the Drive copy
rather than by editing the notebook.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 7: Yanlış LoRA yönergesini düzelt

**Files:**
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/instructions.md` (satır 8, 23)
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/manual.ipynb` (hücre 0 ×2, hücre 8, hücre 10)
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/indirilecekler.md` (satır 47-48, 52, 74)

**Neden ayrı task:** yeni notebook'un çalışmasına engel değil, ama düzeltilmezse bir sonraki kullanıcı 2.28 GiB gereksiz indirip çıktısını bozar. Bulgu: SmoothMix T2V v3 model sayfası *"Just as T2V v2.0 it has light2xv baked in it"*; kullanıcı UI'da sıfır LoRA ile daha iyi sonuç aldı; API export'u 6 step / cfg 1 + boş Power Lora Loader ile çalışıyor.

- [ ] **Step 1: `instructions.md` üstteki uyarıyı ters çevir**

Satır 8'deki blockquote'u sil, yerine:

```markdown
> ⚠️ **LoRA EKLEME.** Yazar Power Lora Loader'ları (**109** / **110**) boş bırakmış ve sampler 6 step / cfg 1'e ayarlı — bu ikisi çelişmiyor: SmoothMix T2V v3'te lightx2v checkpoint'e **merge edilmiş** (model sayfası: *"Just as T2V v2.0 it has light2xv baked in it"*). Elle bir distill LoRA daha eklersen iki kez uygulanır ve çıktı bozulur. Boş bırak.
```

- [ ] **Step 2: `instructions.md` Kullanım adımını düzelt**

Satır 23'teki "Power Lora Loader'lara lightx2v LoRA'larını ekle" adımını sil, yerine:

```markdown
6. **Power Lora Loader 109/110'a dokunma** — boş kalacaklar (yukarıdaki uyarı).
```

- [ ] **Step 3: Notebook hücre 0'daki iki satırı düzelt**

`**Power Lora Loader 109/110'a lightx2v LoRA'larını EKLE** → prompt yaz → Run.` satırını:

```markdown
> **"Choose Your Workflow" → TEXT2VIDEO**'yu aç → prompt yaz → Run.
```

`⚠️ LoRA'ları eklemezsen (sampler 6 step / cfg 1) çıktı çöp olur…` satırını:

```markdown
> ⚠️ **LoRA ekleme.** T2V v3'te lightx2v checkpoint'e merge edilmiş; Power Lora Loader 109/110 boş kalmalı. Elle eklersen iki kez uygulanır ve çıktı bozulur.
```

- [ ] **Step 4: Notebook hücre 8'den lightx2v indirmelerini çıkar**

`HF_MODELS`'taki iki `wan2.2_t2v_lightx2v_4steps_lora_v1.1_*` satırını ve üstlerindeki 2 satırlık yorumu sil. Markdown hücre 7'deki `~37 GiB` → `~35 GiB`.

- [ ] **Step 5: Notebook hücre 10'daki print'i düzelt**

```python
print("⚠️  Power Lora Loader 109/110 BOŞ kalmalı — T2V v3'te lightx2v merged, elle eklemek iki kez uygular")
```

- [ ] **Step 6: `indirilecekler.md`'yi düzelt**

Satır 47-48'deki iki lightx2v manifest satırını sil. Satır 52'deki blockquote'u:

```markdown
> **lightx2v LoRA'sı gerekmiyor.** Yazar Power Lora Loader **109**/**110**'u boş bırakmış ve sampler 6 step / cfg 1 — çelişki değil: T2V v3'te distill LoRA checkpoint'e merge edilmiş (model sayfası: *"light2xv baked in"*). Elle eklemek iki kez uygular ve çıktıyı bozar. 18 Tem 2026'da UI'da doğrulandı: sıfır LoRA en iyi sonucu veriyor.
```

Satır 74'teki grup envanterinde `Power Lora Loader **109**/**110** (boş)` ifadesi zaten doğru — **değiştirme**.

- [ ] **Step 7: Eski yönerge kalmadı mı**

```powershell
Select-String -Path collab-toolbox/video_generator/wan22-smooth-t2v/* -Pattern "lightx2v.*EKLE|LoRA'lar[iı]n[iı] .*ekle|çıktı çöp olur" -CaseSensitive:$false
```
Beklenen: **hiç eşleşme yok**.

- [ ] **Step 8: Commit**

```bash
git add collab-toolbox/video_generator/wan22-smooth-t2v/instructions.md collab-toolbox/video_generator/wan22-smooth-t2v/manual.ipynb collab-toolbox/video_generator/wan22-smooth-t2v/indirilecekler.md
git commit -m "fix(wan22-smooth-t2v): the LoRA instruction was backwards

Every doc here told the operator that leaving Power Lora Loader 109/110 empty
would produce garbage, since the sampler runs 6 steps at cfg 1 and that needs a
distill LoRA. The premise was right and the conclusion was wrong: SmoothMix T2V
v3 already ships lightx2v merged into the checkpoint, so adding it by hand
applies it twice.

Confirmed from both ends. The model page says 'Just as T2V v2.0 it has light2xv
baked in it', and the operator tested many configurations in the UI and got the
best result with no LoRA at all.

The two lightx2v downloads go with it -- 2.28 GiB that made the output worse.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

---

### Task 8: Colab'da uçtan uca doğrulama

**Files:**
- Modify: `collab-toolbox/video_generator/wan22-smooth-t2v/instructions.md` (gerekirse ölçülen değerler)

**Bu task kullanıcı tarafından elle yapılır** — Colab A100 gerekir. Kabul kriteri: **CONFIG'e prompt yaz → Run all → Drive'da video var.**

- [ ] **Step 1: Drive'ı hazırla**

`MyDrive/TextToVideo/` oluştur, repodaki `workflow_api.json`'u içine kopyala.

- [ ] **Step 2: Run all**

Colab → Upload notebook → `api.ipynb` → A100 → CONFIG'e prompt yaz → Run all.
Beklenen: gated probe 2 asset için geçer, ~33.5 GiB iner, ComfyUI 90 sn içinde kalkar, üretim biter, `output/` altında zaman damgalı `.mp4`.
`POST /prompt` `node_errors` verirse → mesajdaki node id'ye bak; graf değişmiş olabilir.

- [ ] **Step 3: Videoyu kontrol et**

Drive'dan aç. Beklenen: prompt'la alakalı, 480×720, 5 sn, 32 fps.

- [ ] **Step 4: Tekrar kullanımı doğrula (asıl kabul kriteri)**

`PROMPT`'u değiştir, **sadece 6. hücreyi** çalıştır.
Beklenen: ikinci video ~1 dk içinde iner, birincisi Drive'da **durur**, dosya adları farklı.

- [ ] **Step 5: Seed davranışını doğrula**

Aynı prompt'la 6. hücreyi bir kez daha çalıştır.
Beklenen: basılan seed farklı, çıktı farklı, iki dosya da duruyor. Aynı seed'i `generate(PROMPT, seed=...)` ile verince aynı video çıkmalı.

- [ ] **Step 6: Ölçülen değerleri yaz**

İlk çalıştırma süresi ve tekrar-render süresi `instructions.md`'deki tahminlerle uyuşmuyorsa gerçek değerlerle güncelle. **Tahmin etme — run'da ne olduysa onu yaz** (drift yasağı).

---

## Self-review notu

- **Spec kapsamı:** spec'in her bölümü bir task'a bağlı — mimari→T1-T5, model manifesti→T3, parametre basma→T5, hata politikası→T5, `instructions.md`→T6, "yan iş"→T7, doğrulama→T8.
- **Tip tutarlılığı:** `fetch(..., *, parallel, headers)`, `check_safetensors(path) -> (str, str)`, `describe_comfy_error(status) -> (str, str, bool)`, `generate(prompt, seed=None) -> str` — T1'de tanımlandığı gibi T3/T5'te kullanılıyor.
- **Bilinçli sadeleştirme:** `loop_maker`'ın `PLAN` / passthrough / `MAX_CONSECUTIVE_FAILURES` / resume mantığı taşınmadı (tek render, YAGNI). Timeout'lu `wait()` ve `describe_comfy_error` taşındı.
- **Test gerçeği:** burada birim test yok. Lokalde doğrulanabilen tek şey notebook'un JSON geçerliliği ve dosya içeriklerinin tutarlılığı; asıl doğrulama Colab'da elle çalıştırmadır (T8). Plan bunu yalanlamıyor.
- **Bilinen belirsizlik:** T5 Step 3'teki hücre sayımı — başlık hücresi tek başına olduğu için toplam **13** (1 + 6×2).
