# WAN 2.2 arbuzai I2V — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `collab-toolbox/video_experiments/wan22-arbuzai/` klasörünü oluştur — v5.0 grafiğinin birebir kopyası + IMAGE2VIDEO grubunun modellerini indiren, Colab'da çalışan self-contained notebook.

**Architecture:** Kardeş klasör `wan22-smooth-t2v` şablon; workflow.json byte-identical kopyalanır, notebook kopyalanıp **sadece** model listeleri (`CIVITAI_MODELS` / `HF_MODELS`) ve UI yönergeleri (markdown + son hücrenin print'leri) değiştirilir. Helper'lar, `fetch()`, `civitai_probe()`, 16'lık `CUSTOM_NODES` ve başlatma hücresi hiç dokunulmadan taşınır.

**Tech Stack:** Jupyter notebook (Colab), ComfyUI, aria2c/curl, cloudflared. Kod yok — test framework yok; doğrulama statik kontrol (hash, JSON parse, grep) + kullanıcının elle yaptığı Colab run'ı.

**Kaynak spec:** [docs/superpowers/specs/2026-07-18-wan22-arbuzai-design.md](../specs/2026-07-18-wan22-arbuzai-design.md)

## Global Constraints

- **`docs/superpowers/` COMMIT EDİLMEZ.** Her commit'te `git add`'e **açık dosya yolu** verilir; `git add .` / `git add -A` **yasak**.
- **Branch:** mevcut `feat/wan22-smooth-t2v` — yeni branch açılmaz.
- **`workflow.json` tek byte değişmez.** Kopya, kaynağıyla aynı sha256'yı vermeli (`9feefc6f…`).
- **Python çalıştırılmaz.** Dosya incelemek Read/Grep; JSON doğrulaması PowerShell `ConvertFrom-Json`.
- **Dil:** notebook markdown hücreleri ve runtime çıktısı (`print`/`log`/`assert`/`RuntimeError`) **Türkçe**; kod yorumları ve docstring'ler **İngilizce**.
- **Yorum kuralı (CLAUDE.md):** yorum WHY anlatır, drift yasak — `# OLD:` / `# NEW:` yok, yorum kodun **şu anki** halini anlatır.
- **`instructions.md` / `indirilecekler.md` bu turda YAZILMAZ.** Kullanıcının UI'da ihtiyaç duyduğu her ayar notebook'un markdown hücrelerine ve son hücrenin print'lerine girer.
- **Notebook self-contained:** hiçbir hücre `wan22-smooth-t2v/` klasöründen dosya okumaz.
- **Kapsam dışı (bu turda yapılmayacak):** `deneme-listesi.md`'ye satır ekleme, `CLAUDE.md` güncellemesi, Drive entegrasyonu, toplu üretim, grafik özelleştirmesi.

**Grafikten doğrulanan gerçekler** (Grep ile `wan22-smooth-t2v/workflow.json` üzerinde teyit edildi — tahmin değil):

| Grafiğin istediği dosya adı | Node |
|---|---|
| `SmoothMix_I2V_v2_High.safetensors` | UNETLoader 197 |
| `SmoothMix_I2V_v2_Low.safetensors` | UNETLoader 186 |
| `umt5_xxl_fp8_e4m3fn_scaled.safetensors` | CLIPLoader 192 |
| `Wan2_1_VAE_fp32.safetensors` | VAELoader 191 |

`clip_vision_h.safetensors` grafikte geçiyor ama yalnız FIRST2LASTFRAME grubunda (CLIPVisionLoader 351) → **inmez**.

---

### Task 1: Klasör + workflow.json birebir kopya

**Files:**
- Create: `collab-toolbox/video_experiments/wan22-arbuzai/workflow.json` (kaynak: `collab-toolbox/video_generator/wan22-smooth-t2v/workflow.json`)

**Interfaces:**
- Consumes: —
- Produces: `wan22-arbuzai/workflow.json` — Task 2'nin notebook'unun markdown'da "bu klasördeki workflow.json" diye işaret ettiği dosya.

**Neden komut:** byte-identical kopya Read+Write ile yapılamaz (yeniden encode/satır sonu riski) ve sha256 özeti dosya okumakla hesaplanamaz → `Copy-Item` + `Get-FileHash` şart.

- [ ] **Step 1: Klasörü oluştur**

```powershell
New-Item -ItemType Directory -Force collab-toolbox\video_experiments\wan22-arbuzai
```

Beklenen: klasör yolu yazdırılır (zaten varsa da hata vermez).

- [ ] **Step 2: workflow.json'u kopyala**

```powershell
Copy-Item collab-toolbox\video_generator\wan22-smooth-t2v\workflow.json collab-toolbox\video_experiments\wan22-arbuzai\workflow.json
```

Beklenen: çıktı yok (sessiz başarı).

- [ ] **Step 3: sha256 birebir mi — doğrula**

```powershell
Get-FileHash -Algorithm SHA256 collab-toolbox\video_generator\wan22-smooth-t2v\workflow.json, collab-toolbox\video_experiments\wan22-arbuzai\workflow.json | Select-Object Hash, Path
```

Beklenen: **iki satır, aynı Hash**, `9FEEFC6F` ile başlıyor. Farklıysa DUR — kopyalama bozulmuş, commit etme.

- [ ] **Step 4: Commit (açık dosya yolu; `git add .` yasak)**

```bash
git add collab-toolbox/video_experiments/wan22-arbuzai/workflow.json
git commit -m "feat(wan22-arbuzai): v5.0 grafiğinin birebir kopyası"
```

- [ ] **Step 5: docs/ sızmadı mı — doğrula**

```bash
git show --stat --name-only HEAD
```

Beklenen: **tek dosya** — `collab-toolbox/video_experiments/wan22-arbuzai/workflow.json`. `docs/` altında herhangi bir yol görünürse commit'i düzelt (`git reset --soft HEAD~1` → tekrar açık yolla add).

---

### Task 2: wan22-arbuzai.ipynb — kardeş notebook'un I2V uyarlaması

**Files:**
- Create: `collab-toolbox/video_experiments/wan22-arbuzai/wan22-arbuzai.ipynb` (kaynak: `collab-toolbox/video_generator/wan22-smooth-t2v/wan22-smooth-t2v.ipynb`)

**Interfaces:**
- Consumes: Task 1'in `workflow.json`'u (yalnız markdown'da referans — notebook onu okumaz).
- Produces: Colab'da Run all ile çalışan notebook. Hücre sırası: 0 başlık(md) → 1 preset(md, **yeni**) → 2 CONFIG başlığı(md) → 3 CONFIG(code) → 4 helpers başlığı(md) → 5 helpers(code) → 6 custom node başlığı(md) → 7 custom node(code) → 8 model başlığı(md) → 9 model(code) → 10 başlat başlığı(md) → 11 başlat(code).

**Kopyala-sonra-düzenle yaklaşımı:** notebook JSON'unu elle yazma. Önce dosyayı kopyala (metadata/kernelspec korunur), sonra **yalnız 4 hücreyi** NotebookEdit ile değiştir ve 1 markdown hücresi ekle. Değişiklikler **aşağıdan yukarı** yapılır (index kayması olmasın), ekleme en sona bırakılır.

Değişmeyen hücreler (kaynaktan birebir taşınır, dokunma): CONFIG başlığı + CONFIG kodu (cookie aynı), helper başlığı + helper kodu (`log`/`human`/`head_text`/`run`/`check_safetensors`), custom node başlığı + kodu (16'lık `CUSTOM_NODES`), başlat başlığı(md), başlat kodunun `print` bloğu dışındaki her satırı.

- [ ] **Step 1: Notebook'u kopyala**

**Neden komut:** kaynak notebook'u byte olarak taşımak (metadata + colab accelerator ayarı dahil) Read+Write'la yeniden yazmaktan güvenli.

```powershell
Copy-Item collab-toolbox\video_generator\wan22-smooth-t2v\wan22-smooth-t2v.ipynb collab-toolbox\video_experiments\wan22-arbuzai\wan22-arbuzai.ipynb
```

- [ ] **Step 2: Kopyayı oku, hücre index'lerini teyit et**

Read: `collab-toolbox/video_experiments/wan22-arbuzai/wan22-arbuzai.ipynb`

Beklenen 11 hücre: `0` md başlık · `1` md "## 1) CONFIG" · `2` code CONFIG · `3` md "## 2) Ortak Yardımcılar" · `4` code helpers · `5` md "## 3) ComfyUI…" · `6` code custom nodes · `7` md "## 4) Modeller…" · `8` code modeller · `9` md "## 5) Başlat…" · `10` code başlat. Farklıysa aşağıdaki index'leri gördüğüne göre düzelt.

- [ ] **Step 3: Hücre 10 (başlat) — yalnız print bloğunu değiştir**

NotebookEdit ile hücre 10'un içindeki şu blok:

```python
print(f"\n🔗 ComfyUI linki: {link}\n")
print("⬆️  Linke gir → bu klasördeki workflow.json'u yükle → 'Choose Your Workflow' → TEXT2VIDEO'yu aç")
print("⚠️  Power Lora Loader 109/110'a lightx2v LoRA'larını EKLE (sampler 6 step/cfg 1 → distill LoRA şart)")
print("    → PromptGenerator'a (230:229) prompt yaz → Run")
print("    Eksik node olursa: Manager → Install Missing Custom Nodes → Restart\n")
```

şununla değiştirilir:

```python
print(f"\n🔗 ComfyUI linki: {link}\n")
print("⬆️  Linke gir → bu klasördeki workflow.json'u yükle → 'Choose Your Workflow' → IMAGE2VIDEO'yu aç")
print("🖼️  LoadImage 287'ye kendi görselini yükle")
print("⚠️  Power Lora Loader 201 (HIGH): lightx2v_high 3.0 + SmoothMix_Animations_XXX_High 1.0")
print("⚠️  Power Lora Loader 200 (LOW) : lightx2v_low  1.5 + SmoothMix_Animations_XXX_Low  1.0")
print("    201=HIGH / 200=LOW eşleşmesi konum tahmini — UI'da teyit et (soldaki HIGH)")
print("    Sampler: 4-6 step, cfg 1, Euler a/Normal ya da UniPC/Simple · çözünürlük 900×600")
print("    Trigger word: SmoothMixAnime / SmoothMixRealism")
print("    → prompt yaz → Run")
print("    Eksik node olursa: Manager → Install Missing Custom Nodes → Restart\n")
```

Hücrenin geri kalanı (cloudflared indirme, pkill, `--enable-manager` ile Popen, 90 sn `/system_stats` bekleme, tünel linki regex'i, `tail -f` ile açık kalan blok ve onun İngilizce yorumları) **aynen kalır**.

- [ ] **Step 4: Hücre 8 (kod, modeller) — model listelerini değiştir**

Hücrenin başındaki `# === Target folders ===`'dan `civitai_probe` tanımının sonuna kadar olan her şey (yani `fetch()`, `civitai_url()`, `cookie_header()`, `civitai_probe()` ve tüm İngilizce docstring'leri) **birebir korunur**. Değişen kısım `WAN22 = …` satırından itibaren:

```python
# === HuggingFace models (aria2c) ===
WAN22 = "https://huggingface.co/Comfy-Org/Wan_2.2_ComfyUI_Repackaged/resolve/main/split_files"
WAN21 = "https://huggingface.co/Comfy-Org/Wan_2.1_ComfyUI_repackaged/resolve/main/split_files"

HF_MODELS = [
    # (url, target_dir, filename, label)
    # The graph ships Power Lora Loader 201/200 empty and I2V v2.0 does NOT have lightx2v merged
    # (the author states so on the model page) -> at 4-6 steps / cfg 1 the distill LoRA is
    # mandatory. Same pair imageToVideo.ipynb already downloads.
    (f"{WAN22}/loras/wan2.2_i2v_lightx2v_4steps_lora_v1_high_noise.safetensors", LORA, "wan2.2_i2v_lightx2v_4steps_lora_v1_high_noise.safetensors", "Lightx2v I2V HIGH"),
    (f"{WAN22}/loras/wan2.2_i2v_lightx2v_4steps_lora_v1_low_noise.safetensors",  LORA, "wan2.2_i2v_lightx2v_4steps_lora_v1_low_noise.safetensors",  "Lightx2v I2V LOW"),
    # VAE: VAELoader 191 asks for 'Wan2_1_VAE_fp32.safetensors', so land the base under that name
    (f"{WAN21}/vae/wan_2.1_vae.safetensors",                          f"{COMFY}/models/vae",           "Wan2_1_VAE_fp32.safetensors",            "Wan2.1 VAE"),
    (f"{WAN21}/text_encoders/umt5_xxl_fp8_e4m3fn_scaled.safetensors", f"{COMFY}/models/text_encoders", "umt5_xxl_fp8_e4m3fn_scaled.safetensors", "UMT5-XXL"),
]

# === Civitai gated models (curl + login cookie) ===
# Civitai serves these under its own file names; UNETLoader 197/186 ask for
# SmoothMix_I2V_v2_High/Low.safetensors, so they land under the name the graph expects --
# imageToVideo.ipynb downloads the same two version IDs under the same two names.
# Model 2040641 ships eight versions (Animation / Futanaris and Males / XXX Animations / Style,
# each High+Low); arbuzai's generation data names the XXX Animations pair.
CIVITAI_MODELS = [
    # (version_id, target_dir, filename, label)
    (2513182, DIFF, "SmoothMix_I2V_v2_High.safetensors",        "SmoothMix I2V v2 HIGH"),
    (2513186, DIFF, "SmoothMix_I2V_v2_Low.safetensors",         "SmoothMix I2V v2 LOW"),
    (2376136, LORA, "SmoothMix_Animations_XXX_High.safetensors", "SmoothMix Animations XXX HIGH"),
    (2376143, LORA, "SmoothMix_Animations_XXX_Low.safetensors",  "SmoothMix Animations XXX LOW"),
]
```

Bu bloğun altındaki üç adım (`1) Fail-fast: probe` / `2) HuggingFace` / `3) Civitai — parallel=False…`) ve özet print bloğu kaynaktaki gibi **aynen kalır** — yorumları dahil.

- [ ] **Step 5: Hücre 7 (markdown, "## 4) Modeller") — I2V metnine çevir**

```markdown
## 4) Modeller — önce gated probe, sonra indir (~36 GiB)

Gated erişim **ağır indirmeden önce** doğrulanır (ilk 1 KB): cookie ölmüşse 27 GiB'lık checkpoint indirmeye başlamadan, Civitai'nin **gerçek yanıtıyla** durur.

Herhangi bir dosya bozuk/eksik inerse hücre `RuntimeError` ile durur; bozuk dosya **silinmez**, inceleme için diskte kalır.

Dosyalar Civitai'nin/HF'in kendi adıyla değil, **grafiğin istediği adla** iner: UNETLoader **197**/**186** `SmoothMix_I2V_v2_High.safetensors` / `_Low` arıyor, VAELoader **191** ise `wan_2.1_vae.safetensors`'ı `Wan2_1_VAE_fp32.safetensors` adıyla bekliyor. Ad tutmazsa dropdown'da model görünmez.

**İnmeyen (bilerek):** T2V checkpoint'leri, `clip_vision_h.safetensors` (yalnız FIRST2LASTFRAME'in CLIPVisionLoader 351'i kullanıyor), MMAudio 4 dosya. TEXT2VIDEO / FIRST2LASTFRAME / AUDIO2VIDEO gruplarını açarsan bunlar eksik olur.
```

- [ ] **Step 6: Hücre 0 (markdown, başlık) — I2V metnine çevir**

```markdown
# WAN 2.2 arbuzai I2V — ComfyUI interaktif deneme (Colab)

**Input:** Kendi görselin + prompt (ComfyUI UI'da) · **Output:** I2V video (yazarın showcase'leri 900×600)

Sıra:
1. **CONFIG** — Civitai cookie
2. **Ortak Yardımcılar** — log + fail-loud run + model doğrulama
3. **ComfyUI + Manager + custom node'lar** (16)
4. **Modeller** — önce gated probe, sonra indir (~36 GiB)
5. **Başlat + cloudflared tünel** → UI linki

> Drive kullanılmaz; modeller her oturumda kaynaktan iner (Colab geçici diski).
>
> **Runtime → Change runtime type → A100** → **Run all** → en alttaki linke gir →
> bu klasördeki `workflow.json`'u yükle → **"Choose Your Workflow" → IMAGE2VIDEO**'yu aç →
> **LoadImage 287**'ye görselini yükle → **Power Lora Loader 201/200**'e LoRA'ları ekle →
> prompt yaz → Run.
>
> ⚠️ LoRA'ları eklemezsen (sampler 4-6 step / cfg 1) çıktı çöp olur — model kötü değil, ayar eksiktir.
> Ayarların tamamı bir sonraki hücrede.
```

- [ ] **Step 7: Index 1'e yeni markdown hücresi ekle — arbuzai preset**

NotebookEdit `insert`, `cell_type: markdown`, index `1` (başlık hücresinin hemen altı, CONFIG başlığının üstü):

```markdown
## arbuzai preset — UI'da ne ayarlanacak

Aşağısı **yazarın (DigitalPastel) model sayfalarındaki beyanı**, bizim ölçümümüz değil. Test ettikçe bu tablo gerçek gözlemle güncellenir.

| Ayar | Değer |
|---|---|
| Checkpoint | UNETLoader **197** = `SmoothMix_I2V_v2_High`, **186** = `SmoothMix_I2V_v2_Low` |
| Girdi görseli | LoadImage **287** |
| Power Lora Loader **201** (HIGH) | `wan2.2_i2v_lightx2v_..._high_noise` **3.0** + `SmoothMix_Animations_XXX_High` **1.0** |
| Power Lora Loader **200** (LOW) | `wan2.2_i2v_lightx2v_..._low_noise` **1.5** + `SmoothMix_Animations_XXX_Low` **1.0** |
| Steps / CFG | **4-6 step**, **cfg 1** |
| Sampler / Scheduler | **Euler a / Normal** ya da **UniPC / Simple** |
| Çözünürlük | 480×720–720×480; I2V v2.0 showcase'leri **900×600** |
| Trigger word | `SmoothMixAnime` / `SmoothMixRealism` |
| Prompt / Seed | arbuzai paylaşmamış ("External Generator") — kendi prompt'unu yaz |

**Bilinmezler ve tuzaklar:**

- **201=HIGH / 200=LOW eşleşmesi konum tahmini** (soldaki HIGH). UI'da teyit et.
- **Distill LoRA aynı LoRA değil.** Yazarın showcase'leri WAN 2.1 dönemi `lightx2v_I2V_14B_480p_cfg_step_distill_rank128_bf16`'yı kullanıyor; burada inen WAN 2.2 `wan2.2_i2v_lightx2v_4steps_lora_v1_*`. 3.0/1.5 birebir taşınmayabilir — hareket aşırı/eksikse önce ağırlığı oynat.
- **I2V v2.0'da lightx2v merged DEĞİL** (T2V v2.0/v3.0'da baked in). Sürümler arası geçişte distill LoRA'yı eklemeyi/çıkarmayı unutmak bozuk çıktının en olası sebebi.
- **XXX Animations dataset'i NSFW poz kapsıyor.** Nötr bir sahnede LoRA'nın etkisi görünmez ve "LoRA çalışmıyor" sanılır.
- **Hiper-gerçekçi içerik morph olabilir** — I2V v2.0 "SmoothMix Animations" stiline çekiyor; gerçekçi hedefliyorsan realizme iten prompt gerekir.
```

- [ ] **Step 8: Notebook geçerli JSON mu — doğrula**

**Neden komut:** .ipynb'nin bozulmadığını Read ile göremezsin; Colab'a yükleyip öğrenmek geç olur.

```powershell
Get-Content collab-toolbox\video_experiments\wan22-arbuzai\wan22-arbuzai.ipynb -Raw | ConvertFrom-Json | Select-Object -ExpandProperty cells | Measure-Object | Select-Object Count
```

Beklenen: `Count : 12` (11 + eklenen preset hücresi). Hata verirse JSON bozulmuş — DUR.

- [ ] **Step 9: T2V kalıntısı kalmadı mı — doğrula**

Grep (`output_mode: content`) `collab-toolbox/video_experiments/wan22-arbuzai/wan22-arbuzai.ipynb` üzerinde:

```
SmoothMix_T2V|WAN_General_NSFW|2768924|2768944|2073605|2083303|109/110|t2v_lightx2v|32 fps
```

Beklenen: **0 eşleşme**. Eşleşme varsa o hücre eksik düzenlenmiş.

> `TEXT2VIDEO` ve `PromptGenerator` bu listede **yok, olmamalı**: ikisi de doğru notebook'ta geçiyor — `TEXT2VIDEO` "4) Modeller" hücresindeki "şu grupları açarsan model eksik olur" cümlesinde, `PromptGenerator` ise değişmeyen `CUSTOM_NODES` listesinde `comfyui-adaptiveprompts`'un satır sonu yorumunda. Aramaya eklenirlerse doğru notebook yanlış diye raporlanır.

- [ ] **Step 10: Yeni model listeleri yerinde mi — doğrula**

Grep (`output_mode: content`, `-o`) aynı dosyada:

```
2513182|2513186|2376136|2376143|SmoothMix_I2V_v2_(High|Low)|i2v_lightx2v_4steps|IMAGE2VIDEO
```

Beklenen: dört version ID'nin her biri ≥1 kez · `SmoothMix_I2V_v2_High` ve `_Low` ≥2'şer kez (preset tablosu + `CIVITAI_MODELS`) · `IMAGE2VIDEO` **tam 2 kez** (başlık md + son print; "4) Modeller" hücresi inmeyen grupları sayıyor, orada geçen kelime `TEXT2VIDEO`) · `i2v_lightx2v_4steps` ≥2 kez (`HF_MODELS`'ın iki satırı).

- [ ] **Step 11: Commit (açık dosya yolu; `git add .` yasak)**

```bash
git add collab-toolbox/video_experiments/wan22-arbuzai/wan22-arbuzai.ipynb
git commit -m "feat(wan22-arbuzai): IMAGE2VIDEO modellerini indiren Colab notebook'u"
```

- [ ] **Step 12: docs/ sızmadı mı — doğrula**

```bash
git show --stat --name-only HEAD
git status --short
```

Beklenen: commit'te **tek dosya** (`…/wan22-arbuzai.ipynb`). `git status` çıktısında `docs/` **untracked (`??`)** olarak kalmalı — staged/committed değil.

---

## Doğrulama — kullanıcı elle yapar (Colab)

Kod tarafında test yok; kabul kriteri **UI'da IMAGE2VIDEO grubuyla kullanıcının kendi görselinden bir video üretilmesi**. Sırasıyla:

1. Notebook A100 runtime'da **Run all** → hata yok, gated probe 4 Civitai asset için geçer, `trycloudflare` linki basılır.
2. Link açılır, klasörün `workflow.json`'u yüklenir → **"missing node" hatası yok**.
3. UNETLoader **197**/**186** dropdown'larında `SmoothMix_I2V_v2_High` / `_Low` görünür (rename doğru çalıştı mı).
4. "Choose Your Workflow" → **IMAGE2VIDEO**. LoadImage **287**'ye görsel. Power Lora Loader **201**: lightx2v_high 3.0 + Animations_XXX_High 1.0; **200**: lightx2v_low 1.5 + Animations_XXX_Low 1.0. Prompt → Run.
5. Video çıkar → arbuzai'nin çıktılarına benziyor mu, **gözle** değerlendirilir.
6. Benzemiyorsa sırayla: LoRA ağırlıkları / yazarın distill LoRA'sı → içerik uyumu (NSFW dataset) → realizm morph'u.

Run'dan öğrenilen her değer preset hücresine yazılır — **tahmin değil, run'da ne olduysa o** (drift yasağı).

**Bu turda çözülmeyen, run'dan gelecek açık uçlar:** `flownet.pkl` (RIFE) otomatik iniyor mu; 201/200'ün HIGH/LOW eşleşmesi.
