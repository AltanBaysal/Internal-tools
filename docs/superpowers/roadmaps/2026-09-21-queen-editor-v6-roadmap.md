# Queen Editor — Yol Haritası v6

**Tarih:** 2026-09-21 · **Koşu dalı:** `feat/queen-editor-v6` · **Durum:** 9/16
**Koşudan çıkan dört madde:** 254 *(kullanıcı yanlış görmüş, belirti yok)*, 256 *(ölçülmüş problem
yok — FOUNDATION 3)*, 258 *(kullanıcı kararı: ayrı çıktılara hiç dokunulmuyor)* ve 260 *(kullanıcı
kararı: çözünürlük seçilmiyor, 1080 otomatik)*. Numaraları kendilerinde kalır; geri gelirlerse aynı
numarayla gelirler.

**21 Eylül'ün son kararı koşunun şeklini değiştirdi** *(kullanıcı — "biz ayrı çıktılara hiç
ellemeyelim, ne değişiklik yaptıysan geri alalım; sadece birleşik çıktıya disclaimer ekleyelim,
yatayda dolduracak şekilde")*: **disclaimer yalnız birleşik export'ta kalıyor.** Ayrı export
249'dan önceki hâline dönüyor — kopyalama, dikey, bindirme yok — çünkü o ölçüde disclaimer
okunmuyor ve kullanıcı okunur bir tasarım gelene kadar oradan vazgeçti. Bu, 249'u geri alan
**261**'i doğurdu ve 258'i düşürdü.
**Öncesi:** [v5](2026-09-11-queen-editor-v5-roadmap.md) — 36/36 kapandı ve `497b0a3e` ile main'e
birleşti.
**Kaynak:** İki madde de kullanıcının 21 Eylül'deki sözlerinden doğdu, backlog'a hiç uğramadan.
[BACKLOG.md](../../../queen-editor/BACKLOG.md)'deki işler yerinde duruyor: bu koşu küçük
*(kullanıcı — "v6 küçük bir koşu olucak", "bu kadar")*.

**Neden v6.** Dal sayacı: `git branch -a` queen-editor için `v1`–`v5` gösteriyor ve beşi de main'e
girmiş, sonuncusu 20 Eylül'de. Sıradaki dal `feat/queen-editor-v6` — bu belgenin başlığında yazan
dal.

**Numara kimliktir, sıra değildir.** 248 v5'in son maddesiydi; bu koşu 249'dan başlıyor.

**Sıra bağımlılığa göre: 249, 250, 251.** 249 ile 250 aynı bindirmeyi istiyor ama aynı saati
istemiyor: tekli export süreyi videonun kendi başından sayıyor, birleşik export birleşmiş zaman
çizgisinden. 249 bindirmeyi kuruyor, 250 yalnız saatini değiştiriyor — bindirme yokken sayılacak bir
şey de yok. **251 sonradan geldi** *(kullanıcı, 21 Eylül, 249'un ortasında — "hatta workflowları da
oraya koyabilirsin")* ve sona kondu: kimseyi beklemiyor, ama 249'un açtığı klasörün var olmasını
bekliyor. **252, 251'in yolunda çıktı** ve sona girdi — defterin hangi dalı klonladığı 251'in defter
hücresine dokunurken görüldü. En sonda olması yerinde: kullanıcının koşu sonundaki testi ona bağlı,
ve öncesindeki maddelerin hepsinin commit'lenmiş olması gerekiyor.

## Nasıl koşulacak

**Her madde iki tur.** Önce yalnız testler: spec → plan → testleri yaz → commit; takım kırmızı kalır.
Sonra implementasyon: spec → plan → kodu yaz → commit; takım yeşile döner.

**Kullanıcıdan gereken tek şey disclaimer PNG'si**, ve o 249'un ilk turunun spec'inin başında
istenir — koşunun ortasında değil, başında. Dosya gelmeden kod başlamaz: PNG'nin kendi ölçüsü,
bindirmenin ekranda kapladığı yeri belirleyen şey.

---

| # | İş | Bitti sayılır |
|---|---|---|
| 249 | ✅ **Tekli export'ta disclaimer.** *(Kullanıcı, 21 Eylül — "exportlara disclaimer eklenecek ... ben sana png olarak atıcam", "videonun başında ilk 1 dakika", "ekranın alt ortasında olacak".)* Sözü edilen düğme export ekranındaki **Videoları ayrı export et** *(`separate`)*; ekranda tam iki düğme var, başka bir video indirme yeri yok *(`ExportScreen.jsx`)*. **Bugün export videonun içine hiç dokunmuyor:** her kare kendi mp4'ü olarak yazılıyor ve akışlar kopyalanıyor — `piece()`, `-c copy` *(`data/ffmpeg_video_exporter.py`)*. **Olacak:** kullanıcının verdiği PNG videonun üstüne biniyor, **ekranın alt ortasında**, videonun **başından itibaren 60 saniye**. Bir karenin videosu grafiğin verdiği sabit uzunlukta — bugün ~5 saniye, ve uzunluk koddan değil grafikten geliyor *(`domain/usecases/export_summary.py`)* — yani 60 saniye pratikte videonun tamamını kaplıyor. Süre yine de **60 olarak yazılır**: grafiğin uzunluğu değişince kuralın değişmemesi için. **Duruşu, kullanıcıyla 21 Eylül'de align olundu:** PNG **videonun genişliğinin %80'i** olacak şekilde ölçeklenir *(sabit piksel değil: ölçü bu depoda bir kez değişti — 218 yatay yaptı, 228 dikeye döndürdü)*, alt kenardan **yüksekliğin %4'ü** kadar yukarıda durur *(dibe yapışan yazı telefonda oynatıcı çubuğunun altında kalıyor)*, ve **PNG'nin kendi saydamlığına dokunulmaz**. **Videoların hepsi taşır**, yalnız ilki değil: 22 dosyadan birinde duran disclaimer'ın orada durmasının anlamı kalmıyor. **Bedeli, ve kaçışı yok:** bindirme kopyalamayı bitiriyor, video yeniden kodlanıyor; export'un bugünkü saniyeler süren hızı buradan gidiyor. Kodlama ayarları spec'te seçilir. **Fotoğraflara dokunulmuyor:** export klasörüne videoların yanında çıkan fotoğraflar *(`domain/usecases/run_export.py`)* olduğu gibi kalıyor — istenen şey videonun başı. **Kullanıcıdan gereken** *(spec'in başında istenir)*: disclaimer PNG'si — **dikey kareye göre kesilmiş** hâli. İlk verilen dosya *(21 Eylül, `1902 × 98`)* bu iş için kullanılamadı: tek uzun bir şerit, ve 480 genişlikte %80'e ölçeklenince üst satırın ~100 karakteri 384 piksele giriyor, yani karakter başına ~3,8 piksel — o boyutta yazı okunmuyor, ve pikseller orada olmadığı için videoyu büyük ekranda açmak da kurtarmıyor. **Kullanıcı kararı, 21 Eylül:** aynı metin **4-5 satıra** bölünüp yaklaşık **960 × 300** verilecek *(480'in iki katı, küçültülünce net kalsın)*. Dosya **depoya commit'lenir** — defter depoyu klonluyor ve hiçbir şey build etmiyor; nereye ve hangi adla gireceği spec'in kararı. **Kapandı** *(`c87e6ebb` kırmızı, `746e4a1c` yeşil)*. **Yeniden kesim gelmedi:** kullanıcı aynı dosyayı kullandırdı *(21 Eylül — "abi dosya zaten group 5 png, o yani")*, yani disclaimer bugün alt ortada ince bir şerit ve yazısı okunmuyor. Bilerek bırakıldı, ve bedeli yok: resim koda girmiyor, `config.DISCLAIMER_PATH`'in gösterdiği dosya — 4-5 satırlık versiyon geldiğinde tek dosya değişiyor, hiçbir satır değişmiyor. Yeri de kullanıcının seçimi: `queen-editor/assets/disclaimer.png` *("queen editor altında asset diye klasör açıp koyabilirsin")* — 251 o klasörden doğdu. | Ayrı dosyalar olarak alınan bir export'tan çıkan mp4 açıldığında disclaimer ekranın alt ortasında duruyor; video kendi ölçüsünü ve üstündeki sesi kaybetmemiş. |
| 250 | ✅ **Birleşik export'ta disclaimer ilk 1 dakika.** *(Kullanıcı, 21 Eylül — "hem tekli exportlar için hem de birleşik exportta".)* **Bugün birleşik mod parçaları makinenin kendi diskinde kesiyor**, sonra `concat` ile **kopyalayarak** birleştiriyor — hiçbir kare yeniden kodlanmıyor *(madde 235; `merge()`)*. **Olacak:** disclaimer **birleşmiş dosyanın ilk 60 saniyesinde** duruyor, 60. saniyeden sonra görünmüyor. Saat parçanın değil **birleşmiş videonun** saati: her parça kendinden öncekilerin toplam süresini devralıyor, yani ~5 saniyelik karelerle ilk on iki kare bindirmeyi taşıyor, sonrakiler taşımıyor. **Sınır karesi, kullanıcıyla 21 Eylül'de align olundu:** disclaimer **tam 60. saniyede** kalkar, o kare ortasında da olsa *(kullanıcı — "bu önemli bir soru değil gibi 1 olsun")*. Bugünkü ~5 saniyelik karelerle 60 zaten on ikinci karenin bitişi; karar grafiğin uzunluğu değişince iş görecek. **Bir karar spec'e kalıyor:** yeniden kodlanmış parçalarla kopyalananların yan yana konmasının `concat`'in kopyalamasını bozup bozmayacağı. Yön belli: **kopyalama korunmaya çalışılır**, çünkü birleşik export'un hızını taşıyan şey o. **Kapandı** *(`ddaec988` kırmızı, `eefcab2e` yeşil)*, ve o risk hiç doğmadı: bindirme parçalara değil **birleştirmenin kendi çağrısına** bindi — `concat` girdisinin üstüne, aynı ffmpeg okumasında. Parçaların hepsi temiz kopya kaldı, kodlama birleşmiş akışın üstünde bir kez oldu, ve `t` birleşmiş videonun saati olduğu için 60. saniye kararı parça sınırına hiç bakmadan çalışıyor. **Yolda bir yalan düzeltildi:** ölçüler uyuşmadığında çıkan cümle *"birleştirme yeniden kodlamıyor"* diyordu — bu maddeyle kodluyor. Cümle artık gerçekten yanlış olanı söylüyor: tek dosyanın tek oranı olur, ve hangisinin istendiğini kimse söyleyemez. | Birleşik dosya açıldığında disclaimer ilk 60 saniyede ekranın alt ortasında duruyor, 60. saniyeden sonra görünmüyor, ve dosya baştan sona tek parça oynuyor. |

| 251 | ✅ **Grafikler de `assets/` altına girecek.** *(Kullanıcı, 21 Eylül, 249 koşarken — "hatta workflowları da oraya koyabilirsin".)* 249 disclaimer için `queen-editor/assets/` açtı; beş grafik JSON'u hâlâ `queen-editor/` kökünde duruyor — `workflow_api.json`, `workflow_video_api.json`, `workflow_video_first_last_api.json` ve iki H3 grafiği. **Olacak:** beşi de `assets/`'e taşınıyor. Yollarını söyleyen tek yer `config.py`'nin beş sabiti; onların dışında dosya adını **defterin bir hücresi** *(üçünü adıyla anıyor)* ve **üç test dosyası** taşıyor. **Bu madde davranışı hiç değiştirmiyor**, yani bitişini gösteren şey de ekranda bir şey değil: takımın yeşil kalması, ve kökte JSON kalmaması. | `queen-editor/` kökünde grafik JSON'u kalmamış, beşi `assets/` altında, dört satır yeşil, ve defterin grafikleri anan hücresi hâlâ duran dosyaları adlıyor. **Kapandı** *(`762fa021` kırmızı, `aae187d3` yeşil)*. `test_producer_contract.py` kendi üç yolunu bıraktı ve `config`'in sabitlerine geçti: taşımanın onu kırması, yolun neden tek evi olması gerektiğinin kanıtı. |

| 252 | ✅ **Defter koşulan dalı klonlayacak.** *(251'in yolunda çıktı, 21 Eylül.)* Defterin CONFIG hücresi `BRANCH = "feat/queen-editor-v5"` diyor, ve `test_notebook_clones_its_branch.py` aynı adı taşıyor — **ikisi de v5 koşusunun denemesinden kalmış**. O dosyanın kendi kuralı *"merge'den önce ikisi de `main`'e döner"* diyor; v5 merge'inde dönmemişler. **Sonucu bu koşuyu doğrudan vuruyor:** Colab depoyu klonluyor, yani defter bugün açılırsa v5'in ağacını indirir ve v6'nın üç maddesinin hiçbiri görünmez — kullanıcının koşu sonundaki testi yanlış kodu test eder. **Olacak:** iki yer birden `feat/queen-editor-v6` olur. **Ve borç yazıya geçer:** merge'den önce ikisi de `main`'e döner, bu maddenin satırında. | Defter ve testin sabiti aynı dalı söylüyor, o dal bu koşunun dalı, ve dört satır yeşil. **Kapandı** *(`f96765e3` kırmızı, `94b61baf` yeşil)*. Sabit v6'ya döndüğünde **iki test birden** düştü — ikincisi *"defterde başka dal adı kalmamış"*, çünkü defterdeki v5 o anda ikinci ad oldu; ikisi de defterin tek satırıyla yeşile döndü. **Ödenecek borç:** merge'den önce defterin `BRANCH` satırı ve `test_notebook_clones_its_branch.py`'nin sabiti birlikte `main`'e döner. |

| 253 | ✅ **Kodlama GPU'ya geçecek.** *(Kullanıcı, 21 Eylül — "kodlamayı gpu ya ver".)* **Kullanıcının ölçümü:** birleşik export T4'te **5 dakikadan fazla** *"birleştiriliyor"*da kaldı; kendi CPU'lu makinesinde normal bir editör aynı işi 2-3 dakikada yapıyor. 249 ile 250 kodlamayı kaçınılmaz kıldı — bindirme yeni bir görüntü — ama kodlamayı **CPU'ya** verdiler *(`libx264`, Colab'da 2 vCPU)*, ve aynı kutudaki **T4 export boyunca boş duruyor**. **Olacak:** kodlayıcı `h264_nvenc` olur. Bindirmenin kendisi, saati, ölçüsü ve disclaimer'ın duruşu değişmiyor — değişen yalnız işi yapan donanım. **Colab'ın ffmpeg'inin NVENC ile derlendiği doğrulanmadı** *(kullanıcıdan istenen çıktı gelmedi)*, o yüzden kodlayıcı **varsayılmıyor, soruluyor:** ffmpeg'e bir kez hangi kodlayıcıları taşıdığı sorulup `h264_nvenc` varsa o, yoksa bugünkü `libx264` seçiliyor. Yanlış varsayımın bedeli her export'un *"Unknown encoder"* ile düşmesi olurdu. **Kaldıraçların tamamı ve bedelleri** [2026-09-21 export hızı araştırmasında](../research/2026-09-21-queen-editor-export-hizi.md): bu madde oradaki **K1**, ve belge ondan önce **üç sayının ölçülmesini** istiyor — NVENC var mı, iş kaç dakika, ve iki kodlayıcının hızı. **Kullanıcıdan gereken, spec'ten önce:** o üç satırın çıktısı. K6 **255** oldu ve K3 **256** *(kullanıcı, 21 Eylül)*; ikisi de bu maddeden sonra, çünkü ikisinin de değeri bu maddenin bıraktığı süreye bağlı. K2 *(CPU ayarını sertleştirmek)* madde değil: NVENC çıkmazsa bu maddenin kendi yolu olur. **Kodlayıcı sorulduğu için bu madde ölçümü beklemiyor.** Ölçüm yalnız kazancın ne kadar olduğunu söyleyecek; hangi kodlayıcının kullanılacağını ffmpeg'in kendi cevabı söylüyor, ve iki durumda da doğru çalışıyor. **Kapandı** *(`f8418549` kırmızı, `12509932` yeşil)*. Soramayan bir makine *"NVENC'siz"* sayılıyor — bir varsayım yüzünden bütün bir export'u kaybetmek, yavaş bir export'tan kötüdür *(FOUNDATION 1)*. **Kazanç hâlâ ölçülmedi**, ve buraya sayı yazılmıyor: kullanıcının sıradaki export'u söyleyecek. **Birleştirmenin şekli değişmiyor:** kullanıcının istediği *"önce birleştir, sonra son videoya ekle"* 250'de zaten böyle — parçalar temiz kopya, disclaimer birleşmenin üstünde; ve tek geçiş olduğu için iki adımdan da hızlı. | Kodlanan her şey — ayrı export'un parçaları ve birleşik export'un birleşmiş akışı — NVENC'li bir makinede `h264_nvenc` ile kodlanıyor, NVENC'siz bir makinede bugünkü gibi `libx264` ile; kodlayıcı ffmpeg'e parça başına değil bir kez soruluyor. |

| 254 | ❌ **Koşudan çıktı — belirti yoktu.** *(Kullanıcı, 21 Eylül — "ben yanlış görmüşüm yatay vermiyormuşsun".)* Madde bir yanlış görmeye dayanıyordu, ve **kod da onu söylüyordu:** bindirme çıkan videonun ölçüsünü değiştiremez, çıkan ölçü kaynağın ölçüsüdür — sebep bu yüzden bulunamadı, çünkü yoktu. Aranan `ffprobe` da artık gerekmiyor. Numara 254 olarak kalır; geri gelirse aynı numarayla gelir. **Altındaki asıl istek düşmedi:** kullanıcı videoların TikTok biçiminde çıkmasını istiyor, ve o bir düzeltme değil yeni bir karar — **258** olarak duruyor — o bir bozulmayı düzeltmiyor, TikTok'un `9:16`'sına geçiyor, çünkü bugünkü `480 × 720` ve `512 × 768` **2:3** ve hiçbir zaman 9:16 değildi. **Teşhis metni silindi:** dayanağı kalmayan bir teşhis belgede durursa, duran bir yanlış olur. | — *(koşulmadı)* |

| 255 | ✅ **Export ekranı disclaimer adımını söyleyecek.** *(Kullanıcı, 21 Eylül — "akışta da disclaimer eklenirken disclaimer ekleniyor densin yani".)* Bugün birleşik export'ta ekran **iki şey söylüyor**: kaç parça yazıldığı, sonra *"birleştiriliyor"*. Kullanıcı o son durumda **beş dakikadan fazla** bekledi ve ekran bu sürenin tamamı boyunca aynı cümleyi gösterdi — ne ilerleme, ne hangi işin sürdüğü. Araştırmanın **K6**'sı: kazancı sıfır saniye, ve buna rağmen şikâyetin yarısı. **Olacak:** disclaimer'ın bindiği adım ekranda kendi adıyla görünür. **Karar, kullanıcıyla 21 Eylül:** **yalnız adımın adı** — *"birleştiriliyor"* yerine disclaimer'ın bindiği adımda *"Disclaimer ekleniyor"*. **Yüzde yapılmıyor**, ve bu sıraya bağlı bir karar: GPU'dan sonraki süre ölçülmedi; birleştirme 20 saniyeye indiyse yüzdenin değeri kalmıyor, hâlâ dakikalarsa yüzde kendi maddesi olur ve asıl çözüm o olur. Yüzde ayrıca exporter'ın ffmpeg'i bekleme biçimini değiştiriyor — bugün `subprocess.run` bitmesini bekliyor — yani ucuz da değil. **Tekli export'a dokunulmuyor** *(bizim kararımız)*: orada her parça bindirme taşıyor, yani *"yazılıyor"* ile *"disclaimer ekleniyor"* aynı şey, ve ekranda sayaç zaten dönüyor. | Birleşik export koşarken ekran, parçalar yazıldıktan sonraki adımda disclaimer'ın bindiğini söylüyor, ve bunu tutan bir test var. **Kapandı** *(`08737b3d` kırmızı, `dd69c082` yeşil)* — **tek satır**, ve arka uca hiç dokunulmadı: adımı `run_export` zaten bildiriyor *(`state="merging"`)*, ve bir durumun hangi cümleyle göründüğü ekranın işi *(CODE-STANDARD; FOUNDATION 4 tersini istemiyor, tarayıcı burada bir kural hesaplamıyor)*. Adım iki iş yapıyor — birleştirme ve bindirme — ve **süreyi yiyen** söyleniyor: birleştirme `concat` kopyası, kodlama ise disclaimer'ın bedeli. **Kırmızı bir değil bir buçuk çıktı:** ikinci test *(ayrı export bu cümleyi söylemiyor)* doğuştan yeşildi ve bilerek bırakıldı — cümle iki modun ortak yerine konursa kırmızıya döner; sonra uygulama turunda ilk testin kendi hatası ortaya çıktı, koşan düğmeye **adını** soruyordu, ve koşan bir düğmenin söylemediği tek şey o. Gösterilen cümleden soruldu. | 

| 256 | 🔁 **Geri geldi — ölçüm geldi.** *(Kullanıcı, 21 Eylül, koşu sonundaki testinden sonra — "beğenmediğim kadar uzun sürdü".)* Madde FOUNDATION 3 gereği düşmüştü: *ölçülmüş bir problem olmadan optimizasyon yok*, ve 253'ten sonraki hiçbir export ölçülmemişti. **Şimdi ölçüldü** — sayı değil ama yargı, ve bu madde için yeterli olan o: kullanıcı bekledi ve rahatsız oldu. Belgenin sözü *"sıradaki birleşik export hâlâ rahatsız ederse madde kendi numarasıyla geri gelir"*di; **aynı numarayla geri geliyor.** Düşürülürken alınan riskin karşılığı da artık belli: **259 kodlanan çerçeveyi karelerin 2,25 katına çıkardı**, yani filtrelenen piksel sayısı dört katına çıktı ve bu maddenin kazancı büyüdü. **281'den sonra koşuyor:** kartın filtreleri alıp almadığı, bu maddenin ne kadar kazandıracağını değiştiriyor — 281 tutarsa kazanç küçülür, tutmazsa bu madde tek gerçek kaldıraç olur. Aşağıdaki metin teşhis değil **tasarım**, ve koşu oradan başlıyor. **Yalnız ilk dakika kodlanacak, gerisi kopyalanacak.** *(Kullanıcı, 21 Eylül — "zaten uzun videoda sadece ilk dakikaya eklenecek ya", "önce videoyu oluştur sonra disclaimer ekle".)* Araştırmanın **K3**'ü. Disclaimer ilk 60 saniyede; kalan bölüm bindirme taşımıyor, yani teorik olarak kopyalanabilir — 5 dakikalık bir videoda karelerin beşte biri kodlanır. **Kazanç videonun uzunluğuyla doğru orantılı, ve bir dakikalık projede sıfırdır.** **Bedeli iki tane, ikisi de gerçek:** kodlanmış bölümle kopyalanmış bölümü tek `concat`'te yan yana koymak ikisinin kodlayıcı parametrelerinin birebir aynı olmasını istiyor *(250 bu riski bilerek doğurmadı)*, ve kesme noktasının bir keyframe'e düşmesi gerekiyor. Parçalar birleştirmeden önce zaten ayrı dosya olduğu için **sınır bedavaya bir parça sınırına düşürülebilir** — ilk 60 saniyeye giren parçalar bindirmeyle kodlanır, sonrakiler kopyalanır. **Bu madde 253'ten sonra açılır ve açılmayabilir:** NVENC kodlamayı gerçek zamanın kat kat üstüne çıkarırsa kazanılacak süre kalmaz, ve o zaman bu maddenin tek getirdiği şey `concat` riski olur. **Açan ölçüm:** 253'ten sonraki export süresi, ve tipik proje uzunluğu. | Birleşik export'ta ilk 60 saniyeye girmeyen hiçbir kare yeniden kodlanmıyor, çıkan dosya baştan sona tek parça oynuyor, ve disclaimer yine tam 60. saniyede kalkıyor. |

| 257 | ✅ **NVENC'in derlenmiş olması kullanılabilir olması değil.** *(Kullanıcı sorusu, 21 Eylül — "bir collabte gpu yoksa cpuya geri düşüyor mu".)* **Cevap: düşmüyor.** 253 ffmpeg'e `-encoders` soruyor, ve o çıktı ffmpeg'in **NVENC ile derlendiğini** söylüyor — makinede kart olduğunu söylemiyor. Kartsız bir makinede ffmpeg `h264_nvenc`'i listeleyip kodlamayı koşarken düşürebilir *(`Cannot load libcuda`, `No capable devices found`)*, ve o anda **tüm export kaybolur**. **Bugün bu hâle düşülmüyor**, çünkü defterin CONFIG hücresi `nvidia-smi`'ye bakıp kartsız oturumu *"GPU yok"* diye durduruyor — ama kontrolün kendisi yine yanlış soruyu soruyor, ve kart dışında da düşme sebepleri var: sürücü uyuşmazlığı, NVENC oturum limiti, kartın meşgul olması. **Olacak:** soru *"derlenmiş mi"* olmaktan çıkıp *"bu makine NVENC'le kodlayabiliyor mu"* olur — ffmpeg'e bir kez hiçbir şey yazmayan minik bir deneme kodlaması yaptırılır *(`-f lavfi -i nullsrc -t 0.1 -c:v h264_nvenc -f null -`)*; dönerse kart gerçekten kodluyor, dönmezse CPU'ya düşülür. Aynı yer, aynı tek çağrı, aynı önbellek. Yeteneği varsaymak yerine **denemek** — 253'ün mantığının doğru uygulanmış hâli. **253 kapalı kalıyor ve numarası kaymıyor:** bu onun düzeltmesi, ve düzeltmenin kendi kaydı var. | Deneme kodlaması dönen bir makinede `h264_nvenc`, dönmeyen bir makinede `libx264` kullanılıyor; deneme düşerse export düşmüyor, CPU'da koşuyor; ve deneme parça başına değil bir kez yapılıyor. **Kapandı** *(`0a6920e0` kırmızı, `51293397` yeşil)*. Test turu **onbir** kırmızı verdi, dört değil: çağrıları süzen yardımcı denemeye baktığı için, kod hâlâ `-encoders` sorduğu sürece o çağrı yazma çağrısı sayıldı ve bindirmeli yolun her testi yanlış komutu gördü. Onbirinin hepsi **tek satırla** yeşile döndü. |

| 258 | ❌ **Koşudan çıktı — ayrı çıktılara dokunulmuyor.** *(Kullanıcı, 21 Eylül — "ayrı çıktılar dikey kalıyor", "biz ayrı çıktılara hiç ellemeyelim".)* Madde ayrı export'u TikTok tuvaline taşıyacaktı; kullanıcı **disclaimer'ı oradan tamamen kaldırmayı** seçtiği için tuvalin de sebebi kalmadı — kopyalanan bir video zaten kendi ölçüsünde çıkıyor, ve o ölçü dikey. Araştırma metni duruyor, çünkü tuval geri gelirse oradan başlar. **Ayrı export TikTok ölçüsüne sığdırılacaktı.** *(Kullanıcı, 21 Eylül — "sen işte yine de tiktok standartına geç neyse araştır, tekli videolar için videoyu sığdır oraya", "bütün videolar için, h3'te olsa wan da olsa".)* **Araştırıldı** *([TikTok spec](https://www.clickanalytic.com/blog/tiktok-aspect-ratio-complete-guide/), [ölçüler](https://postfa.st/sizes/tiktok/video))*: önerilen **1080 × 1920, 9:16**; kabul edilen en küçük çözünürlük **540 × 960**; mp4 / H.264; en çok 500 MB. **Bugün üretilen ölçü 9:16 değil:** WAN `480 × 720`, H3 `512 × 768` — ikisi de **2:3**. Ve 480 genişlik TikTok'un alt sınırının **altında**. **Olacak:** ayrı export'un her dosyası 9:16 tuvale **sığdırılıyor** *(kullanıcının sözü "sığdır")*, yani kırpılmıyor — tuvalin artan yerine bant giriyor. **Model ayrımı yok:** WAN da H3 de aynı tuvale çıkıyor. **Kararlaşmadı:** tuvalin kaç piksel olacağı. `1080 × 1920` platformun önerdiği ve *"TikTok standardı"*nın kendisi, ama 480'den 1080'e çıkmak **2,25 kat büyütme** — detay katmıyor, dosyayı büyütüyor. `540 × 960` alt sınırı yeni yeni geçiyor. Büyütmeden kalmak *(`480 × 854`)* alt sınırın altında kalıyor, yani TikTok'un istediği şey olmuyor. **Önerilecek olan `1080 × 1920`**, çünkü maddenin adı bu. **254 ile ilişkisi:** bu madde çıkan ölçüyü **açıkça** yazdığı için, ayrı export'ta yön sorunu sebebi ne olursa olsun ortadan kalkıyor — çıktı artık *"kaynağın ölçüsü neyse o"* değil, adı konmuş bir tuval. | Ayrı export'tan çıkan mp4 `9:16` ve TikTok'un kabul ettiği çözünürlükte; kare kırpılmamış, sığmayan yere bant girmiş; WAN ve H3 aynı ölçüyü veriyor; ve kullanıcı TikTok'ta açıp *"tamam"* demiş. |

| 259 | ✅ **Uzun videoda yatay tuval, ve disclaimer ona göre büyüyecek.** *(Kullanıcı, 21 Eylül — "uzun videolar için se yatay formatı tut, ama disclaimerı da ona göre büyüt, yani videonun yatay olanın, yani asıl videodan büyük olabilir daha uzun olsun".)* Birleşik export **yatay** kalacak, ve disclaimer o tuvale göre büyüyecek — kullanıcı disclaimer'ın **asıl videodan geniş** olabileceğini, *"daha uzun"* durabileceğini söylüyor, yani bandın üstüne de taşabilir. **Bugünkü kural oransal:** disclaimer tuvalin genişliğinin %80'i, alt kenardan yüksekliğin %4'ü. Tuval genişleyince disclaimer **kendiliğinden** büyüyor; bu madde o oranın yatay tuvalde de doğru oran olup olmadığını soruyor. **Söylenmesi gereken bir sonuç var:** kareler dikey üretiliyor *(2:3)*, yani yatay bir tuvalde videonun **iki yanında geniş bantlar** olacak — dikey bir videoyu yatay çerçeveye koymanın başka yolu yok, kırpmak dışında. **Gösterildi ve karar verildi, 21 Eylül:** kullanıcı bantları bilerek kabul ediyor — *"yatay olsun çünkü yatay olarak biz çıktı alıyoruz diğer türlü"*. Yani birleşik video **16:9**, ve tuval `1920 × 1080`'de **sabit** *(260 düştü: "1080 otomatik olsun")*. Video ortaya **sığdırılıyor**, kırpılmıyor; iki yanı bant. **Disclaimer bu maddede gerçekten büyüyor:** 1920'nin %80'i **1536 piksel**, yani yazı ilk kez okunur ölçüye çıkıyor — ve kullanıcının *"asıl videodan geniş olabilir"* sözü tam bunu karşılıyor, disclaimer videonun 720 pikselini aşıp bantların üstüne biniyor. **Ve "doldursun" dedi** *(21 Eylül — "yatayda dolduracak şekilde olmaz mı")*, yani disclaimer tuvalin genişliğini kaplıyor. **Burada bir şey yerine oturuyor:** kullanıcının verdiği PNG `1902 × 98`, yani **1920 genişlikte bir yatay video için** tasarlanmış — 1080 seçiminde tuval `1920 × 1080` olduğu için dosya neredeyse hiç ölçeklenmeden oturuyor ve iki satır yazı `~49` piksel yükseklikte, okunur. Dikeyde okunmamasının sebebi de buydu: 480 genişliğe sıkıştırılıyordu. **O yüzden bu maddede oran değişiyor:** %80 yerine **tuvalin tam genişliği**. **Ve bu madde hiçbir ölçümü beklemiyor:** tuval açıkça yazıldığı ve sığdırma kaynağın ölçüsünden hesaplandığı için, bugünkü birleşik dosya dikey de olsa yatay da olsa sonuç aynı çerçeve — bu yüzden *"zaten yatay mı"* sorusunun cevabı maddeyi değiştirmiyor. Koddan okunan cevap yine kayda geçsin: birleştirme **kendi tuvalini kurmuyor**, parçaların ölçüsünü alıyor *(`merge()`'in ilk satırı)*, ve parçalar WAN'da `480 × 720` — yani bugün çıkan dosya **dikey**. | Birleşik export'tan çıkan dosya yatay, disclaimer o genişliğe göre büyümüş ve okunuyor, ve kullanıcı bakıp *"tamam"* demiş. **Kapandı** *(`e473a6a8` kırmızı, `877fb7ad` yeşil)* — beş kırmızı, tahminin üstüne bu kez çıkmadı. Tuval artık **devralınan bir sayı değil, yazılmış bir karar**: `scale=1920:1080:force_original_aspect_ratio=decrease` + `pad`, yani kırpma yok, iki yanı bant; disclaimer `scale=1920:-1` ile tam genişlikte. `DISCLAIMER_WIDTH` silindi — 1.0 ile çarpan bir oran, okuyana aramaya değer bir şey varmış gibi görünür. **Bir yalan daha düzeldi:** farklı ölçülerin mesajı *"ikisini birden sığdırmak bant koymak olurdu"* diyordu, ve bant koymak bu maddeyle yaptığımız şey oldu; yerine gerçek sebep yazıldı — `concat` parçaları olduğu gibi okuyor, ölçü değiştirmiyor. **Bedeli açıkça:** kodlanan çerçeve karelerin 2,25 katı; kodlama zaten yapılıyordu ve karta gidiyor, süre hâlâ ölçülmedi. |

| 260 | ❌ **Koşudan çıktı — seçim yok, 1080 otomatik.** *(Kullanıcı, 21 Eylül — "hatta çıktı seçeneğinde vermeyelim, 1080 otomatik olsun".)* Seçenek ekranda bir kutu, spec'te bir parametre ve backend'de bir daldı; kullanıcı üçünü de istemiyor. Birleşik tuval `1920 × 1080`'de **sabit** — PNG'nin tasarlandığı genişlik de o. Metin duruyor, çünkü seçim geri gelirse üç ölçünün karşılıkları oradan okunur. **Birleşik export'ta çözünürlük seçilecekti.** *(Kullanıcı, 21 Eylül — "bence bir de şey lazım kullanıcıya 480 720 1080 seçeneği vermek exportta".)* 258 tuvali **1080 × 1920**'de sabitliyor; bu madde onu **seçime** çeviriyor: kısa kenar 480, 720 ya da 1080. **Seçimin gerçek karşılığı var, sadece bir kutu değil:** kaynak 480 genişlik üretiyor, yani **480 seçimi hiç büyütmüyor** — dosya küçük, kodlama kısa. 1080 ise 2,25 kat büyütme, ve TikTok'un önerdiği ölçü. **720 ortada**, ve TikTok'un `540 × 960` alt sınırının üstünde. **Söylenmesi gereken:** `480 × 854` o alt sınırın **altında**, yani o seçim TikTok için *"kabul edilen en küçük"*ün berisinde kalıyor; seçenek yine duruyor, çünkü her export TikTok'a gitmiyor — ama ekran bunu söylemeli. **Yalnız birleşik export'a uygulanıyor**, ve bu 258 düştüğü için böyle: ayrı export artık kopyalıyor, ve ona çözünürlük seçtirmek onu yeniden kodlamak demek olurdu — kullanıcının *"hiç ellemeyelim"* dediği şeyin tam tersi. **Kararlar, spec'te:** varsayılan **1080** *(`1920 × 1080`, ve PNG'nin tasarlandığı genişlik)*; seçim **koşuya ait**, projeye kaydedilmiyor — export modunun kendisi de öyle. **FOUNDATION 4 gereği kural arkada:** ekran seçimi topluyor, ölçüyü backend'in ucu alıyor; tarayıcı bir ölçü hesaplamıyor. | Export ekranında üç çözünürlük seçilebiliyor, seçilen ne ise çıkan dosyanın kısa kenarı o oluyor, 480 seçiminde hiç büyütme olmuyor, ve ekran 480'in TikTok'un alt sınırının altında olduğunu söylüyor. |

| 261 | ✅ **Ayrı export'tan disclaimer kalkacak — 249 geri alınıyor.** *(Kullanıcı, 21 Eylül — "şimdilik disclaimer yok çünkü okunmuyor, okunacak yeni disclaimer tasarlanacak", "ne değişiklik yaptıysan geri alalım".)* 249 ayrı export'un her parçasına disclaimer bindirdi, ve bunun iki bedeli oldu: **okunmayan bir şerit** *(480 genişlikte satır başına ~10 piksel)* ve **kopyalamanın sonu** *(her parça yeniden kodlanıyor)*. Kullanıcı ikisini de geri istiyor. **Olacak:** `piece()` 249'dan önceki hâline dönüyor — sessiz parça `-c copy`, sesli parça bugünkü ses yolu — ve `run_export` bindirme bayrağını hiç geçmiyor. **Kalkmayanlar:** birleşik export'un bindirmesi *(250)*, kodlayıcının karta sorulması *(253, 257)* ve `assets/disclaimer.png` — üçü de birleşik yolun işi. **Kazanılan şey yalnız sadelik değil, hız:** ayrı export yine hiçbir kareyi kodlamıyor, yani saniyelere dönüyor. **Kaydı kalıyor:** 249'un spec'i ve testleri tarihte duruyor; disclaimer ayrı export'a geri gelirse oradan başlar, ama **okunur bir tasarımla** — o iş [BACKLOG](../../../queen-editor/BACKLOG.md)'da. | Ayrı export'tan çıkan mp4 disclaimer taşımıyor, kaynağın kendi ölçüsünde ve dikey, ve komut yine `-c copy` — yani hiçbir kare yeniden kodlanmıyor. **Kapandı** *(`850a2dbf` kırmızı, `990db935` yeşil)*. Test turu **yirmi dört** kırmızı verdi: bayrak testlerden kalktığı anda ikizlerin `piece()`'i üç argümana döndü, üretim kodu ise hâlâ `disclaimer=` geçiyordu — tek bir `TypeError` bütün export akışını düşürdü. 249'un üç testi **silindi, atlanmadı**: geri alınan bir davranışın testi borç değil, yanlış kayıttır. Yolda iki ölü şey de gitti — kimsenin çağırmadığı `_stamp(video)`, ve modül başlığındaki *"bir parça disclaimer taşıdığı için kodlanıyor"* cümlesi. |

| 281 | **Kartın yaptığı iş kodlamayla kalmayacak — çözme ve filtreler de karta.** *(Kullanıcı, 21 Eylül, testten sonra — "gpu varsa olabildiğince gpu kullanalım abicim cpu değil sağlam gpumuz var zaten".)* **Bugün kart işin yalnız son adımını yapıyor.** `h264_nvenc` **kodlamayı** alıyor *(253, 257)*; ondan önceki üç adım — parçaları **çözmek**, tuvale **sığdırıp bant koymak**, disclaimer'ı **bindirmek** — Colab'ın **2 vCPU**'sunda koşuyor. NVENC ayrı bir donanım bloğu ve 110 saniyelik 1080p onun için saniyelerin işi, yani darboğaz kart değil **önündeki filtre zinciri**. **259 o zinciri ağırlaştırdı:** her kare artık `480 × 720` yerine `1920 × 1080` piksel üzerinde işleniyor, yani yazılımda itilen piksel ~4 kat. **Olacak:** çözme ve filtreler de karta taşınır — `-hwaccel cuda` ile çözme, `scale_cuda`/`scale_npp` ile ölçekleme, ve bindirme kartın kendi bindirmesiyle. Kareler karttan hiç inmezse CPU'nun payı sıfıra yaklaşır. **Varsayılmıyor, deneniyor** — 257'nin kendi dersi: `-encoders` çıktısı *"derlenmiş mi"* sorusuna cevap verir, *"bu makine yapabiliyor mu"* sorusuna vermez, ve yanlış varsayımın bedeli **tüm export'un düşmesi**. Bu maddede de zincirin tamamı minik bir deneme koşusuyla sınanır; dönerse kullanılır, dönmezse bugünkü CPU zinciri aynen kalır *(FOUNDATION 1 — yavaş bir export, kaybolan bir export'tan iyidir)*. **Kararlaşmadı, spec'e:** `overlay_cuda` Colab'ın ffmpeg'inde yoksa yalnız çözme ve ölçeklemenin karta taşınıp bindirmenin CPU'da bırakılması mı yoksa zincirin tamamının CPU'da kalması mı — ikisi arasındaki fark, kareyi karttan indirip geri yüklemenin bedeli. **Çıkan dosya değişmiyor:** aynı tuval, aynı sığdırma, aynı disclaimer, aynı saat. Değişen tek şey işi kimin yaptığı. | Filtreleri taşıyabilen bir makinede çözme, ölçekleme ve bindirme kartta koşuyor; taşıyamayan bir makinede export bugünkü gibi CPU'da koşuyor ve **düşmüyor**; deneme parça başına değil bir kez yapılıyor; ve çıkan dosya 259'un bıraktığı dosyayla aynı. |

| 286 | **Parçaların ses akışı aynı mı — birleştirmenin sormadığı soru.** *(Teşhis bizim, 21 Eylül; koddan okundu, ffmpeg'de doğrulanmadı.)* `merge()` her parçaya **ölçüsünü** soruyor ve uyuşmazsa reddediyor, ama **akış düzenini** hiç sormuyor. Oysa `piece()`'in sessiz dalı `-c copy`: kaynak videonun sesi yoksa parça **ses akışı taşımıyor**, ses katmanı olan kare ise aac taşıyor. `concat` demuxer'ı bütün dosyaların **aynı akışlara** sahip olmasını istiyor — yani **WAN + yalnız bazı karelerde ses** olan bir projede birleştirme bozulabilir, ve `-map 0:a?` bunu düzeltmez, yalnız sesi hiç olmayan bir sete izin verir. **Neden bugüne kadar görünmedi:** H3 videosu kendi sesini taşıyor *(243)*, yani H3 projelerinde bütün parçalar sesli; kullanıcının beklediği export'ta da ya hepsinde ses vardı ya hiçbirinde — ikisi de bunu gizler. **Olacak:** ilk tur teşhisi **çivilemek** — parçaların akış düzeni farklıysa ne olduğunu gösteren bir test — sonra koruma. **Yön spec'e, iki aday var:** sessiz parçaya da sessiz bir ses akışı yazmak *(o parçaların kopyası bozulur, ama aac ucuz)*, ya da `concat` demuxer'ı yerine **concat filtresine** geçmek *(video zaten kodlanıyor; farklı ölçü yasağını da kaldırır, ama sesi de kodlar)*. **Belirti çıkmazsa 254 gibi kapanır:** teşhis yanlışsa metni silinir, numarası kendinde kalır. **FOUNDATION 1 bunu madde yapıyor:** sessizce bozulan bir çıktı, kullanıcının işini kaybetmenin en kötü hâli — hiçbir hata mesajı söylemiyor. | Sesi yalnız bazı karelerde olan bir projede birleşik export ya doğru çalışıyor ya da sebebini söyleyerek duruyor; sessizce bozuk dosya çıkmıyor; ve bunu tutan bir test var. |

| 282 | **Birleşmiş dosya Drive'a damla damla yazılmayacak.** *(Teşhis bizim, 21 Eylül; koddan okundu, ölçülmedi.)* 235 parçaları Drive'dan kaldırıp makinenin kendi diskine aldı — `tempfile.mkdtemp` *([photo_store.py:79](../../../queen-editor/backend/features/photo_generation/data/photo_store.py#L79))* — ama **çıktıyı Drive'da bıraktı:** birleştirmenin hedefi Drive'ın export klasörü, yani **ffmpeg kodlarken çıkan akışı FUSE mount'una parça parça yazıyor** *(`run_export.py`'nin `merge` çağrısı)*. **259'dan sonra bunun ağırlığı arttı:** dosya artık `1920 × 1080`. Ağustos'ta ölçülen soğuk Drive hızı **3,34 MB/s** *([export hızı araştırması](../research/2026-09-21-queen-editor-export-hizi.md))*; 150 MB'lık bir çıktı tek başına ~45 saniye, ve ffmpeg'in küçük küçük yazması toplu bir kopyadan yavaş olabilir. **Ölçülmedi, ve buraya sayı yazılmıyor** — bilinen tek şey yazmanın nereye gittiği. **Olacak:** birleşme `/tmp`'de bitiyor, sonra Drive'a **tek seferde** kopyalanıyor. 235'in parçalar için verdiği kararın çıktıya uygulanmamış hâli. **Yan faydası bedava:** yarıda kalan bir kodlama Drive'daki klasöre hiç dokunmuyor, yani madde 94'ün *"yarım export bitmiş gibi görünür"* kuralı bir adım daha sağlam. | Birleşik export koşarken Drive'daki export klasöründe yarım mp4 hiç görünmüyor; dosya iş bitince tek parça olarak oraya geliyor. |

| 283 | **Tekli ve birleşik çıktılar kendi klasörlerine.** *(Kullanıcı, 21 Eylül — "tekli ve çok videolar için klasör açalım çıktıyı oraya koyalım", ve "ayrı ayrı küçük çıktıları kendi klasörüne alalım çok kalabalık oluyor".)* **Bugün iki mod tek tarihli klasörü paylaşıyor** *(`photo_store.py`'nin kendi yorumu bunu söylüyor)*, ve ayrı export oraya **22 video + 22 fotoğraf** koyuyor: 44 dosya, iç içe. Numaraların eşleşmesi bilerek böyleydi *(236)* ve öyle kalıyor — sorun eşleşme değil, **tek klasörde kalabalık**. **Olacak:** her modun çıktısı kendi klasörüne giriyor. **Kararlaşmadı, kullanıcıya sorulacak** *(spec'in başında)*: klasörlerin adları, ve fotoğrafların videolarla aynı klasörde mi ayrı bir klasörde mi duracağı. **Numaralar değişmiyor**, ve tarihli klasör de yerinde kalıyor: değişen tek şey içindeki düzen. | Ayrı export'un videoları kendi klasöründe, birleşik export'un dosyası kendi klasöründe, ve tarihli klasörü açan kullanıcı 44 dosya yerine iki isim görüyor. |

| 284 | **`pieces.txt` Drive'da durmayacak.** *(Kullanıcının sorusundan çıktı, 21 Eylül — "pieces txt ne".)* `concat`'in okuma listesi **hedefin yanına** yazılıyor *([ffmpeg_video_exporter.py:141](../../../queen-editor/backend/features/photo_generation/data/ffmpeg_video_exporter.py#L141))*, ve birleşik export'un hedefi Drive. Yani dosya export süresince kullanıcının klasöründe duruyor, ve **süreç ortada ölürse orada kalıyor** — `finally` yalnız Python yaşarsa koşar, Colab oturumu gitmişse koşmaz. **Olacak:** liste parçaların yanına, `/tmp`'ye yazılıyor. Parçalar zaten orada *(235)*, ve liste onlardan daha az kalıcı bir şey. **Bir satır**, ve bu maddenin kendi başına durmasının sebebi: bir problem bir madde. **282 ile ilişkisi:** ikisi de aynı satırdaki `os.path.dirname(target)`'a bakıyor, o yüzden 282'den sonra koşuyor — 282 hedefi `/tmp`'ye taşırsa bu madde kendiliğinden çözülebilir, ve o zaman tek işi kaydı yazmak olur. | Birleşik export koşarken Drive'daki klasörde `pieces.txt` hiç görünmüyor, ve export yarıda kesilirse de orada bir şey kalmıyor. |

| 285 | **Ekran V6 diyecek.** *(Kullanıcının göreceği bir yanlış; 21 Eylül.)* Başlık hâlâ **"Queen Editor V5"** yazıyor — [version.js:7](../../../queen-editor/frontend/src/shared/version.js#L7)'de `VERSION = "V5"` — ama v6 gerçek davranış değiştirdi: yatay birleşik çıktı, disclaimer, yeni adım adı. **Olacak:** sayı v6 olur. Sayıyı tutan tek yer o satır, ve testi zaten `/^V\d+$/` diye soruyor *(248'in kararı: sayı iki yerde yazılmaz)* — yani testi değiştirmeye gerek yok, ve bu maddenin ilk turu **o kararın hâlâ geçerli olduğunu** çiviliyor: sayının nereden geldiğini soran bir test. **Ön yüz değiştiği için `dist/` aynı commit'te build'lenir** *(FOUNDATION 3)*. **Borç neden burada:** sürüm numarası koşuyla birlikte artıyor, ve v5 kapanırken artmamış; bu satır o borcu kapatıyor. | Export ekranının başlığı "Queen Editor V6" diyor, ve sayıyı hâlâ tek bir dosya söylüyor. |

---

**İlk dokuz madde kapandı ve kullanıcı test etti** *(döngünün 10. adımı)*. Takımın dört satırı
yeşil — queen-editor 929, ön yüzü 667, queen-agent 923 ve 652.

**Test koşuyu yeniden açtı.** Kullanıcının cevabı *"beğenmediğim kadar uzun sürdü"* oldu, ve
ardından iki istek geldi: *"gpu varsa olabildiğince gpu kullanalım, cpu değil, sağlam gpumuz var
zaten"* ve *"tekli ve çok videolar için klasör açalım, çıktıyı oraya koyalım"*. Konuşurken üç şey
daha çıktı — biri kullanıcının gördüğü bir yanlış, ikisi koddan okunan. **Yedisi de bu tabloya
girdi** *(döngünün 8. adımı: sonradan çıkan iş koşan yol haritasına madde olur, ikinci belge
açılmaz)*.

**Numaralar 281'den devam ediyor.** 262–276 [v7](2026-09-21-queen-editor-v7-roadmap.md)'nin,
277–280 [QueenAgent v9](2026-09-21-queen-agent-v9-roadmap.md)'un — ikisi de biz koşarken yazıldı.
**256 ise kendi numarasıyla geri geldi**, bu belgenin sözü olduğu gibi.

**İkinci yarının sırası: 281 → 286 → 256 → 282 → 283 → 284 → 285**, ve numara sırası değil
bağımlılık sırası. **281 başta**, çünkü kullanıcının açık isteği o ve çıkan dosyayı hiç
değiştirmiyor — ve 256'nın ne kadar kazandıracağı 281'in tutup tutmamasına bağlı. **286, 256'dan
önce**, çünkü ikisi de `concat`'in neyi yan yana kabul ettiğine dayanıyor: 256 kodlanmış parçayla
kopyalanmışı yan yana koyacak, ve o sınırın önce anlaşılması gerekiyor. **282 ile 284 aynı satıra
bakıyor**, o yüzden 284 ondan sonra. **285 kimseyi beklemiyor** ve sonda: tek satır, ve ekranın
söylediği sayı koşu kapanırken doğru olsun.

**Hız ailesinin ölçümü hâlâ eksik**, ve bu bilerek yazılıyor: kullanıcının yargısı *("beğenmediğim
kadar uzun sürdü")* maddeleri açmaya yeter, ama **hangisinin ne kadar kazandırdığını** yalnız
öncesi/sonrası süre söyler. Üç madde de kendi başına doğru çalışıyor; sıralamanın sebebi kazancın
büyüklüğü değil, birbirlerine bağlı olmaları.

**Colab'a girmesi için dalın push'lanması gerekiyor** — defter depoyu klonluyor, push'lanmamış iş
orada görünmez *(FOUNDATION kararı 1)*. **Kullanıcının testi koşunun sonunda**
*(döngünün 10. adımı)*, tek seferde.

**Colab'da denenmeden bilinmeyen üç şey var, ve buraya tahmin yazılmıyor:** bindirmenin gerçek
ffmpeg'de nasıl göründüğü, kartın NVENC'le kodlayıp kodlamadığı, ve export'un **kaç dakika**
sürdüğü. Testler komutu okuyor, bu makinede ffmpeg yok. **Kullanıcının testi üçüncüsüne yargı
verdi ama sayı vermedi** — *"beğenmediğim kadar uzun sürdü"* — ve 256, 281, 282 o yargıyla açıldı.
Sayı hâlâ bir maddeyi bekletiyor: süre bu üçünden sonra da dakikalarsa **yüzde** kendi maddesi olur
*(255'in kararı)*.

**Merge'den önce:** 252'nin satırındaki iki satır `main`'e döner.

**Bir sonraki koşu.** [v7](2026-09-21-queen-editor-v7-roadmap.md) **yazıldı ve kendi dalını
bekliyor** — 262–276, bu koşu sürerken yazıldı ve v6 kapandıktan sonra başlar. v7'nin maddeleri
buraya girmiyor, ve bu koşuya sonradan çıkan iş de oraya gitmiyor: **koşan yol haritası bu**, ve
281–286 o yüzden bu tabloda *(döngünün 8. adımı)*.
